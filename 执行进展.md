# 执行进展 · C→B→A（从后向前）

> 依据《代码改造计划.md》§7「下一步建议顺序」的 A/B/C 三段，按用户指示**从后向前**执行（先 C 纯代码增量，再 B 决策/提交，最后 A QWK 攻坚）。
> 凡需启动 llama.cpp / 真实 LLM 的步骤，**通知用户手动启动**（用户需看实时日志），不自助执行。
> 起始：2026-06-16，分支 `feat/cli`。测试基线：开工前 **142 绿**。

图例：✅ 完成　🟧 进行中　⬜ 待办　🔔 需用户参与（llama.cpp/真实 LLM/合并）　⏸ 故意后置

---

## C 段 · 能力增量（纯代码，不依赖 LLM 运行时）

| # | 条目 | 状态 | 说明 |
|---|---|---|---|
| C5 | 通用文件扩展 / track changes 解析 | ✅ | **track changes 解析**已落（`track_changes.py`，接入 `parse_document`→报告，确定性、不自动扣分）；**通用文件扩展（DocumentParser 接口化）**按设计§5.5 继续后置 ⏸ |
| C4 | 5.2 上线漂移监控（线上抽样复核） | ✅ | `analytics.review_sample`（确定性抽样：need_manual_review 必抽 + hash 补足）+ `drift_monitor`（复核覆盖率 + 漂移结论，覆盖率不足则信号不可信）；端点 `/batches/{id}/review-sample`、`/drift-monitor` |
| C3 | 术语一致性聚类 | ✅ | `coherence/terminology.py`：缩写定义聚类（abbr↔全称冲突，后缀归一化抗过度捕获）+ 英文写法变体聚类（大小写/写法不统一 info）；接入 `analyze_coherence`→报告 |
| C2 | N3 章节识别：PDF 字号/加粗启发式 + 小模型兜底 | ✅(半) | **PDF 字号/加粗启发式**已落（`_extract_pdf` 用 `get_text("dict")`，正文字号取众数，偏大/整行加粗短行判标题）；**小模型兜底**需 scorer 串进上传期 parse + llama 验证 → 留 A 段 ⏸ |
| C1 | 导入侧从 Excel/模板解析 sub_checks（启用 hybrid） | ✅ | Excel「子检查」列 `名称\|类型\|分值` 按行解析→`sub_checks`，提供即自动 `criterion_type=hybrid`；持久化/schema 早已带该字段 |

## B 段 · 决策/提交类

| # | 条目 | 状态 | 说明 |
|---|---|---|---|
| B1 | `SCORING_LLM_DIRECT_SINGLE_CALL` 提交（保持默认 off） | ✅ | 已提交 `9abe426`（独立提交）；翻默认另议（需重锚 mock 端到端） |
| B2 | 真实 LLM 全面验证 | 🔔 | 需 llama.cpp，**通知用户启动** |
| B3 | 分支合并 main（feat/cli、feat/scoring-alignment） | 🔔 | 需用户操作 |

## A 段 · QWK 攻坚（需 llama.cpp，🔔 通知用户启动）

| # | 条目 | 状态 | 说明 |
|---|---|---|---|
| A4 | 跑全量 98 篇出正式基线 | ✅ | **n=98 定论：QWK −0.002≈0**（3.75h，0错0失配，无死亡螺旋）。adj 档一致 0.90、exact 0.45、逐维 bias ≤1.5。混淆矩阵：模型只预测中等/良好两档→无排序力。系统=粗档辅助评分，非排序器 |
| A3 | 干净 A/B 隔离 holistic vs TOP_K | 🔔 | 测量卫生；~35min×2，需用户跑 llama |
| A2 | 首启 L2 锚点 few-shot | ✅ | 策展+注入已跑实测：QWK +0.14（三轮最高但 n=15 噪声内）；副作用抬 R01/R02。代码已提交 `d3649e3` |
| A1 | 修 R03 +6.27 系统偏宽 | ✅(部分) | banding+nudge：R03 bias **6.27→3.0**、总 MAE 7.35→6.72，但 **QWK 未动(≈0)**——R03 无排序信号。保留作 A2 底座（用户定）。代码已提交 `d3649e3` |

---

## 执行日志

### 2026-06-16 · 开工
- 通读三份文档 + QWK 攻坚断点；建本进展文件。测试基线 142 绿。
- 决定 C5 先落 track-changes 解析（确定性切片），通用文件扩展继续后置（§5.5）。
- **C5 ✅**：`document_parser/track_changes.py`（解析 w:ins/w:del + 章节定位 + 汇总 warning）；接入 `parse_document`（docx best-effort，进 coherence_findings→报告，不自动扣分）；`test_track_changes.py` 4 测。**142→146 绿**。
- **C4 ✅**：`calibration/analytics.py` 增 `review_sample`（§15.2 抽样复核：need_manual_review 必抽 + sha256 稳定抽样补足比例，可复现）+ `drift_monitor`（叠加 ReviewLog 复核覆盖率 + 总体结论：覆盖率<门槛→信号不可信）；config 增 `MONITORING_REVIEW_SAMPLE_RATIO/MIN_REVIEW_COVERAGE`；端点 2 个；`test_review_monitoring.py` 4 测。**146→150 绿**。注：复核信号取 ReviewLog（final_score 评分时即回填，不能当复核标志）。
- **C3 ✅**：`coherence/terminology.py`（缩写定义聚类 abbr↔全称冲突 warning，后缀归一化抗正则过度捕获；英文术语大小写/写法变体聚类 info）；接入 `analyze_coherence`→解析→报告；`test_terminology.py` 5 测。语义级近义归并（需向量/LLM）继续后置。**150→155 绿**。
- **C2 ✅(半) ⏸**：`_extract_pdf` 改用 `get_text("dict")` 取字号/加粗 → `_pdf_heading_texts`（正文字号众数，≥1.15× 或整行加粗的≤40字短行判标题），PDF 不再"无样式仅规则"；`test_pdf_heading_heuristic.py` 2 测。**小模型兜底**留 A 段（需 scorer 串入上传期 parse + llama 验证）。**155→157 绿**。
- **C1 ✅**：`rubric_import/parser.py` 增「子检查」列别名 + `_parse_sub_checks`（按行 `名称|类型|分值`，类型含"确定/规则"→deterministic 否则 llm_judgment）+ `_raw_value`（保留换行不被空白折叠）；提供子检查即 `criterion_type=hybrid`；persist/schema 早已带 `sub_checks`，无需迁移；`test_sub_checks_import.py` 3 测。**157→160 绿**。
- **🏁 C 段完成**：C5/C4/C3/C2(半)/C1 全绿，142→**160 绿（+18 测）**；纯代码增量，无新迁移，零回归。两处确定性切片已落、LLM 依赖半（C2 小模型兜底）随 A 段。下一步进入 B 段（决策/提交）。
- **B1 ✅**：C 段成果与 single-call 开关按用户选择**两个独立提交**：`053562b`(C 段能力增量)、`9abe426`(single-call opt-in，默认 off)。config.py 按 hunk 拆分入两提交（临时移除→提交→还原法，避开交互式 add -p）。working tree 干净，**160 绿**。翻默认未做（需重锚 mock 端到端）。
- **⏸ B/A 边界**：B2(真实 LLM 验证)、A1–A4(QWK 攻坚)、C2(小模型兜底) 全需 **llama.cpp** → 按用户要求由用户启动看实时日志；B3(合并 main) 需用户操作。

### 2026-06-16 · A 段（llama 已启动，端口 18434，4 slots，n_ctx 16384）
- **A1 诊断**：从 cli.db 取 R03 每篇 AI 分对比教师真值 → **天花板压缩**：AI R03 全部 ∈{24,27,29}（众数 27、无区分度），教师 R03 ∈[12,24] 均值 20.07 stdev 2.77。`bias==MAE==+6.27` 是因 15 篇全部 AI≥教师。→ **常数偏移校准只修 MAE 不修 QWK**；选 **banding**（破天花板、直击区分度，rubric 授权非写死）。
- **A1 落地**：cli.db 新建 v1.3 rubric `9262a246-3dd0-44da-9994-919af579b206`（克隆 v1.2，仅 R03 → scoring_mode=banded + 5 档带严格 descriptor，优秀 28/良好 23/中等 19/及格 15/不及格 9，校准到教师量程）。**v1.2 冻结未动**。
- **A1 验证①（裸 banding）= 负面结果**：3 篇 R03 全被模型选「优秀/28」(basis=model-band)，bias 仍 +5.67、28>原 27。**模型宽松碾过档位描述、自贴顶档**——天花板压缩的本质是缺负向锚，单靠档位文字喂不进。抽样先行省了 28min。
- **A1 验证②（banding + 反顶档 nudge）= 成功修偏**：给 `_mode_instruction` banded 加"逐条对照 descriptor、就低不就高、先假定中等档、顶档须明确创新+充分验证"硬指令；bump `PROMPT_VERSION=2026-06-16-1`。3 篇 R03 从「优秀/28」降到「良好/23」，**R03 bias +6.27→+0.67、MAE 6.27→1.33**，3 篇总 MAE 7.73→4.47。nudge 是有效成分（裸 banding 不行）。但 3 篇质量相近全判「良好」，**区分度需全 15 篇判**。13 prompt/banded 单测仍绿。
- **A1 验证③（全 15 篇）= 部分胜、QWK 未动**（35min，exit 0，报告 `eval_report_20260616T065533Z.json`、基线 `/tmp/r03band_full15.json`）：
  - **QWK +0.10→−0.11**（n=15 噪声内，两者统计上≈0，无法区分）；**总 MAE 7.35→6.72 ✓**；**R03 bias +6.27→+3.0、R03 MAE 6.27→3.53 ✓**；exact/adj 档一致 0.33/0.93。
  - 其余项稳定（R01 −1.0/R02 +0.67/R04 −0.13/R05 +0.11，与基线一致）→ 干净对照，只动 R03。
  - R03 分布：良好/23 ×16、优秀/28 ×4、中等/19 ×1（有微弱铺开但重压良好，4 篇仍顶档）。
  - **结论**：修 R03 偏宽**降了 bias/MAE 却没动 QWK**——R03 近常数、本不携带排序信号，压下去只是去掉一个有偏维度。**QWK≈0 的真瓶颈是模型对质量缺区分度（各项都压缩），不是 R03 偏宽。** A1 作为"修偏"达成（bias 砍半），作为"撬 QWK"无效。
  - **下一步**：→ **A2 L2 锚点 few-shot**（各档脱敏范文给全维度注入区分度）才是 QWK 真杠杆。A2 需从 98 篇真实论文挑锚点+脱敏+从留出集剔除，**仓库 public 含学生 PII，须用户把关**（见 [[public-repo-pii-incident]]）。
  - **用户决策**：保留 banded R03+nudge 作 A2 底座；A2 先出方案不跑（我搭工具+设计，用户过目后再跑 llama）。

- **A2 方案就绪（不跑，待过目）**：
  - 工具：`services/calibration/curate.py`（`select_anchor_targets` 按教师分分位低/中/高各档选锚点、确定性、**剔除留出集**；`desensitize` 去姓名/学号；`anchor_rationale` 模板）+ `scripts/build_anchors.py`（编排：解析→检索评分项相关段→脱敏→写 CalibrationAnchor，**本地跑、锚点文本只入 cli.db、严禁入库**）。`test_calibration_curate.py` 7 测。**160→167 绿**。注入机制 `get_anchors`（≤4/项注入 prompt）早已有。
  - 执行计划（待批）：① 本地 `python -m backend.app.scripts.build_anchors --rubric <v1.3> --papers-dir 待评分-docx --scores 成绩表-评阅教师.xlsx --holdout 成绩表-抽样15.xlsx --clear`（无 llama，~分钟级，写 15 条锚点=3 档×5 项）；② llama 跑全 15 篇 eval（锚点自动注入），与 A1 的 −0.11 / 基线 +0.10 对照，看区分度是否上来。
  - 待确认参数：每项 3 档（低 0.15/中 0.5/高 0.85 分位）、摘录 600 字、rationale 模板（可人工精修）。

- **A2 策展执行（无 llama）**：本地跑 `build_anchors` → 写 15 锚点（3 档×5 项）到 v1.3，候选池 83 篇、**留出集 15 篇剔除**。
  - **关键发现**：教师逐项真值方差极小（R01 stdev1.62/R03 2.64/R05 0.95）→ 真值本身缺逐项区分度、QWK 结构性难；初版 0.15/0.85 分位锚点对比太弱。
  - **用户定**：拉宽分位 → `DEFAULT_LEVELS=0.05/0.5/0.95`，重策展。R03 锚点 18/22/25、R01 12/15/18、R02 13/15/18、R04 12/15/17、R05 6/7/8。**PII 扫描 0 命中**（脱敏+取正文段生效；R01 锚点为 Abstract 英文）。
- **A2 验证完成**（37min，exit 0，基线 `/tmp/a2_anchors_full15.json`、报告 `eval_report_20260616T092508Z.json`）：

**三轮全 15 篇对照（n=15）**：
| 配置 | QWK | 总MAE | exact档 | adj档 | R03 bias |
|---|---|---|---|---|---|
| 基线 v1.2 holistic | +0.100 | 7.35 | 0.40 | 0.93 | +6.27 |
| A1 v1.3 banded+nudge | −0.114 | 6.72 | 0.33 | 0.93 | +3.0 |
| **A2 +L2 锚点** | **+0.139** | 7.24 | 0.27 | 0.93 | +2.67 |

  - A2 逐维 bias：R01 +2.20 / R02 +1.87 / R03 +2.67 / R04 +0.47 / R05 −0.63。
  - **锚点副作用**：把原本校准好的 R01(−1.0→+2.2)/R02(+0.67→+1.87) **往上抬**（高档锚点 18/20 在模型之上→拉高），R03 仅再降一点点。净效果 MAE 比 A1 略差、exact 档跌到 0.27。
  - **R03 仍塌**：A2 下 R03 = 良好/23 ×14 + 中等/19 ×1，锚点没给 R03 加区分度。
  - **结论（QWK 攻坚阶段性）**：三轮 QWK +0.10/−0.11/+0.14 在 n=15 下**统计无法区分、全≈0**。banding(A1)、anchors(A2) 都没能撬动 QWK。**根因是结构性的**：① 教师逐项真值方差极小(stdev 1~2.6)→真值本身缺排序信号(呼应人类互评 QWK 0.49~0.74)；② 模型在窄带内不区分(R03 恒 23)。模型侧的便宜杠杆(A1/A2)已基本用尽，QWK 仍噪声受限。
  - **adj 档一致率恒 0.93** = 93% 论文系统判分与教师在相邻档内——系统"大方向对、细排序难"，与低方差真值相符。
  - **下一步真正能给可信 QWK 的只有 A4（全 98，n=98 杀噪声）**；A3(holistic vs TOP_K 干净 A/B) 是测量卫生。两者都是大块 llama 时间，需用户定。当前最佳配置 = A2(v1.3 banded R03+nudge+锚点)，QWK 点估计最高、设计最完整。

### 2026-06-16 · A4（全 98 篇可信基线）进行中
- **设计决策**：评全 98 会让 15 锚点篇自评泄漏 → 用户选**方案 B：纯模型全 98、不注锚点**（最干净的"模型本体真实力"，彻底无泄漏）。
- **执行**：删 cli.db 中 v1.3 的 15 条锚点（A2 负面、可一键重建，无损失）→ v1.3 现 anchor-free（保留 banding R03+nudge）。98 篇全部 resolve。后台 `bqjveeucq`，运行配置 single_call+TOP_K8+timeout180/retry2，基线 `/tmp/a4_full98.json`。
- **安全网**：L0 缓存按输入哈希存 → 若 30B 长跑内存压力崩，重跑同命令自动命中已评、断点续，不全废。
- **✅ A4 结果（n=98 定论，报告 `eval_report_20260616T170331Z.json`、基线 `/tmp/a4_full98.json`）**：跑 3.75h（21:17→01:03）、**0 错误 0 失配、无死亡螺旋**（4-slot+timeout180/retry2 稳）。
  - **QWK −0.002 ≈ 0**（n=98 统计可信）；总 MAE 6.83、RMSE 8.78；**exact 档 0.449、adj 档 0.898**；逐维 bias R01+0.45/R02+1.50/R03+1.51/R04+1.30/R05+0.84（全温和，banding 把 R03 偏宽在全量也压到 +1.5）。
  - **混淆矩阵病根**：模型只预测「中等/良好」两档，从不给 不及格/及格/优秀；真值 63/98=中等。→ 全塌中间档、完全不区分 及格↔中等↔良好 → QWK 必≈0（靠猜众数白捡 45% exact）。
  - **整个 QWK 攻坚定论**：①系统**无排序力**（QWK≈0 铁证），模型对质量无区分度；②但**绝对校准不差**（bias≤1.5、MAE~2/项、90% 一档内）；③根因结构性（真值极集中 63/98 中等、人类互评 QWK 0.49~0.74，可榨排序信号本就少；模型又默认中间档丢掉仅有信号）。**QWK~0.5 在此数据大概率不可达**。
  - **产品含义**：系统宜定位"**粗档辅助评分 + 人工复核**"（档位大方向对、90% 一档内），非可信排序器。模型侧调参(A1/A2)已证撬不动 QWK；若仍要追，唯一未试方向是**提高证据覆盖**（A3 holistic vs TOP_K、或喂更多章节），但 A1/A2 提示上限有限。

## 🏁 本轮总收尾（C→B→A 全程）
- **C 段**(能力增量) + **B1**(single-call) + **A 段**(QWK 攻坚 A1/A2/A4) 全部完成。测试 142→**167 绿**，4 提交(`053562b`/`9abe426`/`d3649e3`/`6139ed5` + A4 收尾待提交)。
- **QWK 攻坚画句号**：n=98 定论 QWK≈0，结构性不可达，系统定位粗档辅助。
- **仍待用户**：A3(干净 A/B，可选)；C2 小模型兜底；B2 真实 LLM 全面验证(本轮已大量用 llama 实跑 98+45 篇，实质已验真)；B3 合并 main。
- **本地数据**：v1.3 rubric `9262a246`(banded R03，锚点已删) 在 cli.db，未入库。

# 生产上线与灾备验收清单

本清单是 PGS-12 的可执行门禁。填写人必须保存命令输出、GitHub Actions artifact 或内部等价归档；勾选文本本身不是证据。OPS 绿色是 GATE-03 的必要条件，但始终 `production_default_switch_authorized=false`。

## 1. 责任与变更身份

- [ ] 发布 revision/commit：`________________`
- [ ] 部署环境/站点：`________________`
- [ ] 发布责任人：`________________`
- [ ] 数据库责任人：`________________`
- [ ] LLM/模型责任人：`________________`
- [ ] 值班责任人及升级电话：`________________`
- [ ] GATE-03 candidate hash 与维护者批准：`________________`

## 2. CI 与迁移证据

- [ ] `pgs-unit-and-sqlite-evidence`：Python 3.12、uv.lock、全套 pytest 绿色。
- [ ] `pgs-postgres-migration-restore-evidence`：Postgres CI 绿色，0011→0012→0013→0014→0015→0016→0017→0018→0019→0020→0021→0022→0023 逐段升级报告齐全。
- [ ] Postgres FK/CHECK、Core run 唯一身份、单批次活动任务 partial unique、`ORDER BY created_at, id` 稳定排序验证通过。
- [ ] 0023 新表已向存在的 `pgs_app` 授予最小 DML 权限并启用 `pgs_app_dml` RLS；未授予 DDL、角色或跨 schema 权限。
- [ ] 有 0017 任务数据、0022 回填的组织归属数据或 0023 规则/人工复核审计数据时 downgrade 被明确拒绝，拒绝后 Alembic revision 保持不变。
- [ ] 最终分支已按保护规则合入 main，CI run URL：`________________`
- [ ] `deploy-vercel-production` 只由 `main` push 且前三项门禁成功后运行；Vercel Git 直部署保持关闭。
- [ ] GitHub `production` Environment 中 `VERCEL_TOKEN`、`VERCEL_ORG_ID`、`VERCEL_PROJECT_ID` 已轮换/复核，未出现在日志或 artifact；生产 Job URL：`________________`

## 3. 启动安全与日志

- [ ] `AUTH_ENABLED=true`；AUTH_PASSWORD ≥12 位且非默认/用户名，AUTH_SECRET ≥32 位且非占位。
- [ ] `BYOK_MASTER_KEY` 已从 KMS/独立受控部署密钥提供，且没有回显于容器环境诊断、日志或备份 manifest。
- [ ] 旧单租户升级已核验：0022 默认组织、Bootstrap Admin、历史 batch/report/model 信息和组织归属均正确；没有将任何旧 `.env` LLM Key 复制到 `ai_connections`。
- [ ] `LLM_DEBUG_LOG_ENABLED=false`；抽检日志和 artifact 不含 API key、Authorization、密码、token、论文原文或学生 PII。
- [ ] 云 LLM 使用已批准的数据出境范围；未批准时使用 Mock/本地模型并开启 OFFLINE_MODE。
- [ ] Caddy TLS、HSTS、nosniff、DENY frame、上传限制和健康检查通过。

## 4. 监控阈值（由部署责任人批准）

实际值来自 `.env.intranet`，不得只依赖示例默认值。`GET /api/system/ops-readiness` 仅提供诊断，不授予 Core 切换权限。

| 信号 | 配置 | 批准值 | 告警与动作 |
|---|---|---:|---|
| 磁盘剩余 | `OPS_DISK_FREE_GB_MIN` | `____ GB` | 低于阈值立即停止新上传，扩容/清理已归档产物 |
| 数据库容量 | `OPS_DATABASE_SIZE_GB_MAX` | `____ GB` | 达阈值 80% 预警，100% 阻止发布并扩容 |
| 批任务心跳 | `OPS_BATCH_STALE_MINUTES` | `____ min` | 超时告警，确认旧 runner 失效后再恢复 |
| LLM 失败率 | `OPS_LLM_FAILURE_RATE_MAX` | `____` | 超阈值停止新批次，检查 429/timeout/provider |
| 恢复时间目标 | `OPS_RTO_MINUTES` | `____ min` | 演练实际 RTO 不得超过批准值 |
| 恢复点目标 | `OPS_RPO_MINUTES` | `____ min` | 备份频率与最近成功备份年龄不得超过 RPO |

## 5. 备份恢复演练记录

- [ ] 用 `python -m backend.app.scripts.ops_backup create` 生成 database.dump、完整 storage.tar.gz 和 SHA-256 manifest。
- [ ] 用 `ops_backup verify` 验证后，恢复到隔离 recovery database 与空 storage 目标；不得覆盖当前生产目录。
- [ ] 恢复库 migration head、关键表计数、uploads、DocumentSnapshot 引用、cache/audit ledger 哈希均一致。
- [ ] 演练开始/完成时间：`________ / ________`；实际 RTO：`____ min`；最近备份年龄/RPO：`____ min`。
- [ ] 备份恢复演练记录 artifact/工单：`________________`

## 6. 发布、观察与回滚

- [ ] PGS-6 真实 100 份 Postgres soak、timeout/429/进程重启证据齐全。
- [ ] 本次若修改 LLM 调用构造：已用仓库外真实教师评分留出集运行 QWK，记录命令、聚合报告与回退比较；Mock/test-only 结果不可代替该证据。
- [ ] GATE-03 冻结 dataset/truth、唯一 immutable RubricVersion、model/provider、policy/plan/prompt、anchors/holdout 排除、checker/revision，`gating_eligible=true` 且获维护者批准。
- [ ] 发布后观察窗口、人工复核和值班排班已经开始。
- [ ] 回滚触发：任何批准阈值失败、数据身份不完整、不可解释 delta、重复权威 run、恢复演练失败。
- [ ] 回滚动作：只把未版本化默认入口恢复到 legacy compatibility；停止新批任务；保留不可变历史 run；执行事件复盘。不得改写 published RubricVersion 或历史权威结果。
- [ ] Vercel Production URL 与主域名 `/`、`/api/system/integrations` 均返回 200；部署 URL、alias 与线上静态资源哈希已记录。
- [ ] 若使用 `vercel deploy --prebuilt`，CLI 保持仓库批准版本；不得删除 `.vercelignore` 绕过 filePathMap/ENOENT，升级 CLI 前必须完成独立生产演练。

## 7. 2026-08-22 Vercel/Supabase 生产发布记录

- [x] 发布代码：`main` / `3b544a670066ceec9519c0d3f90bf8cd253eba93`；Vercel 部署 `dpl_8GPV2quDdMnefVonhpUKSr3LcooL`，站点 `https://paper-grading-system-kohl.vercel.app/`，状态 `Ready`。
- [x] CI：GitHub Actions [run 32567324937](https://github.com/liuzhne/paper-grading-system/actions/runs/32567324937) 通过；本次未变更评分核心，`SCORING_ENGINE_MODE=legacy`，未申请或授予 GATE-03/Core 默认切换。
- [x] 数据库：唯一链接 Supabase 项目 `euizniwqisnhfhxwiqsg`，空库初始化并复核到 `0022_legacy_tenant_backfill`；生产复核为 1 个组织、2 个用户、38 条 `pgs_app` RLS 策略。
- [x] 最小权限：`pgs_app` 已验证通过会话池及事务池的读写回滚；无超级用户、建库、建角色、BYPASSRLS、DDL、`auth`/`storage` schema 权限。未来迁移新增表或序列后必须重新授予运行权限和对应 RLS 策略。
- [x] 启动安全：Production 使用受管 Sensitive 变量；`AUTH_ENABLED=true`、邀请制注册、强随机认证凭据、`BYOK_MASTER_KEY`、私有 Supabase 桶均已配置，`LLM_DEBUG_LOG_ENABLED=false`。
- [x] 用户明确授权本次跳过备份：目标库为空库，且不保留旧生产数据。此为本次例外，不改变本清单第 5 节的常规备份恢复要求。
- [x] 冒烟：首页 `200`；公开状态端点 `200`；未认证数据端点 `401`；Bootstrap Admin 登录 `204`；认证后的身份、组织、AI connection、ops-readiness 端点 `200`；私有 Storage 临时对象写入、读取、删除成功。
- [x] 平台托管 LLM 关闭且默认 Mock；`OFFLINE_MODE=false` 仅用于允许组织私有 BYOK 在授权后访问其模型服务。无真实模型调用、无正式评分模板/批次/论文被用于本次验证。
- [ ] 发布后观察窗口、值班责任人和常态化备份恢复演练：仍需由业务/运维负责人补充，不得因本次空库发布而视为完成。

最终批准人：`________________`　日期：`________________`

## 8. 2026-08-28 Vercel 首屏性能修复发布记录

- [x] 发布代码：性能提交 `dev / d902e9962da8c2d0a7a79faedf4dcd2f4bea3acc`，合并提交 `main / 848d2d709e2141b4b4f11ab72f860fa483805601`；两个分支均已推送至 `origin`。
- [x] 生产部署：先验证 Preview `dpl_FBBEPVbusNHo4FsaXvK5amtsxtGD`，再提升为 Production `dpl_Gwr9daApor1vt6VMETYvP4hTfNTu`；主域名 `https://paper-grading-system-kohl.vercel.app/`，状态 `Ready`。
- [x] 区域与连接：Vercel FastAPI Function 已从 `iad1` 调整到 `sin1`，与 Supabase `ap-southeast-1` 同区；运行时继续使用 6543 transaction pooler，未修改数据库凭据。
- [x] 前端交付：首页、登录、注册和重置页由 Vercel CDN 静态交付；JS/CSS 使用内容哈希文件名和 `public, max-age=31536000, immutable`，首页第二次请求验证为 `x-vercel-cache: HIT`。
- [x] 请求链路：首屏独立 API 改为并行请求，移除重复 organizations 加载；会话、用户和组织成员身份由一次 SQL 查询解析。
- [x] 可观测性：API 返回 `Server-Timing`，公开端点验证 `app;dur=2.6ms`；Function 响应 `x-vercel-id` 显示 `sin1::sin1`。
- [x] 本地门禁：Python 3.12 全套测试 `1394 passed`；Vercel 本地生产构建和线上 Preview/Production 冒烟均通过。
- [x] 本次无 Alembic 迁移、无评分核心或 prompt 修改、无数据库/Storage 数据写入；`SCORING_ENGINE_MODE` 保持 `legacy`，未申请或授予 GATE-03/Core 默认切换。

## 9. 2026-08-29 GitHub Actions 生产发布方式切换记录

- [x] 发布方式：Vercel Git 集成直接部署已通过 `vercel.json` 的 `git.deploymentEnabled=false` 关闭；后续唯一生产入口为功能分支合入 `main` 后触发 `pgs-production-gates`。
- [x] 发布代码：中文提示与生产流水线合并提交 `main / df170f9bc3b7ce823d13f004ac12870cde5256f5`；Vercel prebuilt 热修复最终合并提交 `main / 2cc1f07fa7fe940bdeeafab92e11159de92296bd`。
- [x] GitHub Environment：使用名称精确为 `production` 的 Environment；`VERCEL_TOKEN`、`VERCEL_ORG_ID`、`VERCEL_PROJECT_ID` 均已复核，Token 未进入仓库、日志或 artifact。
- [x] 生产门禁与部署：GitHub Actions [run 33242160933](https://github.com/liuzhne/paper-grading-system/actions/runs/33242160933) 的全量测试、Postgres 16 迁移/恢复、Docker Compose 冒烟和 `deploy-vercel-production` 全部通过。
- [x] Vercel CLI：因 `58.4.4+` 的 `deploy --prebuilt` + `.vercelignore`/Python `filePathMap` [上游回归](https://github.com/vercel/vercel/issues/17386)，流水线固定 `vercel@58.4.0`；保留 `.vercelignore`，不得以扩大上传范围作为规避方案。
- [x] 生产结果：deployment URL `https://paper-grading-system-65zpylvmp-derrick-s-projects5.vercel.app` 已 Ready，并 alias 到 `https://paper-grading-system-kohl.vercel.app/`；首页与 `/api/system/integrations` 均验证 HTTP 200，线上资源为 `app.13efb6624a08.js` 且包含中文阻断提示。
- [x] 发布边界：本次没有 Alembic 迁移或评分核心默认切换；`SCORING_ENGINE_MODE=legacy`，未申请或授予 GATE-03/Core 默认切换。

## 10. Supabase 私有桶直传与可恢复解析验收

- [ ] Production 的 `STORAGE_PROVIDER=supabase`，`SUPABASE_URL`、服务端 Secret Key 和私有桶已配置；Secret Key 未进入静态资源、API 响应、浏览器日志或 GitHub artifact。
- [ ] `DIRECT_UPLOAD_MAX_SIZE_MB` 已按业务批准（建议论文材料 50 MiB），`DIRECT_UPLOAD_TUS_THRESHOLD_MB=6`；旧 multipart 的 `MAX_UPLOAD_SIZE_MB` 不得误用于限制浏览器直传。
- [ ] Supabase 私有桶保持非公开；允许站点 Origin 对签名标准上传和 TUS 所需的 `PUT/POST/PATCH/HEAD`、`x-signature`、`Tus-Resumable`、`Upload-*` 头执行 CORS，并暴露 `Location`、`Upload-Offset`。
- [ ] 用 ≤6 MiB 的 DOCX/PDF 验证“标准签名直传 → 对象大小确认 → 单文件解析”；浏览器 Network 中不存在将文件内容发送到 Vercel `/api/papers/bulk-upload` 的请求。
- [ ] 用 >6 MiB 的测试文档验证自动选择 TUS、6 MiB 分块、直连 `project-ref.storage.supabase.co`、进度显示和上传完成后的对象确认。
- [ ] 在小文件标准上传过程中模拟离线/连接重置，验证界面明确提示切换 TUS；恢复网络后从服务端 offset 继续，且同一 Paper 不重复解析。
- [ ] 分别模拟对象缺失、对象大小不符、Storage 5xx、解析失败和解析函数中断；前端显示中文原因与下一步操作，成功文件不被失败文件回滚。
- [ ] `PAPER_PARSE_LEASE_SECONDS` 已按最长正常解析时间批准；活跃 `parsing` 拒绝并发解析，超过租约后“重新解析”可接管，既有 chunks 仅在新解析成功后替换。
- [ ] 存储桶中抽检对象路径按组织/批次/Paper 隔离；完成确认不能修改对象路径或声明大小，不存在跨组织读取/确认。
- [ ] Vercel 日志和运行指标确认上传请求体不再触发 `FUNCTION_PAYLOAD_TOO_LARGE`；Function 只承担小型 JSON 签名、确认与解析控制请求。
- [ ] 发布 revision、CI run、Production deployment、标准上传/TUS/恢复演练证据：`________________`。

## 11. 2026-09-04 评分韧性与人工复核生产发布记录

- [x] 发布代码：功能提交 `0267cbdf0ee610e12af3b35d1771117cda256f9f` 经 [PR #2](https://github.com/liuzhne/paper-grading-system/pull/2) 合并为 `main / 735ac851db187cdba3c3218ae6f46385627c5614`；仓库外论文资料与 `docs/参赛材料/` 未提交、未上传。
- [x] 本地门禁：Python 3.12 全套测试 `1463 passed, 48 warnings`；`compileall`、`git diff --check`、SQLite 全迁移链、0023 降级/重放和有数据 fail-closed 验证通过。
- [x] CI 与部署：GitHub Actions [run 33823617924](https://github.com/liuzhne/paper-grading-system/actions/runs/33823617924) 的锁文件全量测试、Postgres 16 迁移/约束/恢复和 Docker 冒烟全部通过；Production deployment `https://paper-grading-system-4nazfzxub-derrick-s-projects5.vercel.app` 已 alias 到 `https://paper-grading-system-kohl.vercel.app/`。
- [x] 生产数据库：唯一链接 Supabase 项目 `euizniwqisnhfhxwiqsg` 已由 `0022_legacy_tenant_backfill` 事务化升级到 `0023_rule_scoring_review_tasks`；两张新表为空，8 个索引、17 个约束、两条 `pgs_app_dml` RLS 策略和 `pgs_app` 最小 DML 权限均已核验。一次性 Supabase transport marker 已移除，Alembic 继续作为唯一迁移事实源。
- [x] 线上冒烟：主域名 `/` 与 `/api/system/integrations` 返回 `200`；未认证 `/api/v2/manual-review-tasks` 返回 `401`；Bootstrap Admin 登录返回 `204`，认证后人工复核队列返回 `200` 且为空。
- [x] 发布边界：`SCORING_ENGINE_MODE` 未切换，生产 LLM 仍为 Mock；Langfuse/OpenTelemetry metadata-only、脱敏、采样和 fail-open 代码已部署，但因未提供生产 endpoint/public/secret key，Exporter 按设计保持关闭。
- [x] 风险例外：用户明确接受生产风险并授权本次绕过真实 QWK、GATE-03 与完整上线清单；这不等于上述门禁通过，也不授予 Core 默认切换权限。Supabase 显示 WAL-G 已启用但没有可列出的物理备份，本机 Docker 未运行导致 CLI 逻辑导出未生成有效文件；0023 为纯新增表迁移，已依赖事务回滚和迁移后结构核验继续发布。
- [ ] 后续责任：生产负责人仍需配置并验收独立 Langfuse 服务、RBAC/保留/备份策略，补做仓库外真实教师留出集 QWK、GATE-03、备份恢复演练、真实 Groq 故障注入/观察窗口和值班安排；完成前保持 legacy/Mock 安全边界。

## 12. 前端 v2 阶段 0 发布准备（待发布）

发布内容为**新增并存入口**，默认入口 `/` 仍是旧 SPA。本次没有 Alembic 迁移、没有评分内核或 prompt 改动。

- [x] 发布代码：分支 `feat/workbench-v2-redesign`，`5ae9931` → `c5bb2e5`（7 个提交，基于 `main / 02467a7`）。
- [x] 发布边界：**无 Alembic 迁移**，head 保持 `0023_rule_scoring_review_tasks`，不涉及生产数据库结构或数据写入；未触碰 `services/scoring/*`、prompt 构造与 `cache/llm_cache.PROMPT_VERSION`，因此第 6 节的 QWK 要求不适用；`SCORING_ENGINE_MODE=legacy` 不变，未申请也未授予 GATE-03 / Core 默认切换。
- [x] 本地门禁：Python 3.12 全套 `1480 passed`；前端 `11 passed`；`compileall` 与 `git diff --check` 通过；`scripts/build_web_static.py --with-workbench` 重建后产物与提交一致（连续两次重建字节相同，删除 `dist` 后仍一致）。
- [x] 新增入口：`/workbench/*`（Vue 3 SPA）与旧 SPA 并存，资源独立在 `/workbench/assets/*`；深链接刷新回 HTML、缺失资源 404、`/api` 不被 SPA 回退吞掉，均已本地实测。
- [x] 已做实页面仅「账户与连接」；其余 8 页为标注阶段的占位视图，附回旧 SPA 入口。浏览器验收矩阵（V01–V13）尚未建立，`npm run test:e2e` 显式失败退出，不得记为通过。

### 12.1 本次的行为变更与已处理回归

- [x] `/system/ops-readiness`、`/system/llm-check` 由「任意已登录用户」收紧为**平台管理员**；`/release-gates/*` 增加路由级平台守卫。仅收紧角色，不替代 release gate 服务原有的候选身份、门禁条件与人工批准约束。
- [x] 新增 `/system/organization-readiness`（org_admin 或已选组织的平台管理员），先按组织过滤再聚合，不下发磁盘/数据库/部署安全等宿主事实；缺组织上下文直接拒绝，不回落为全局查询。
- [x] **已修复的生产回归**：旧 SPA 的「测试模型连接」按钮对所有登录用户可见且直接调用 `/system/llm-check`，收紧后非平台管理员会拿到 403。已同步按平台角色隐藏该入口，并对 403 给出指向「账户设置 · 我的 AI 连接」的中文说明。用户自带连接（BYOK）的自测走 `/ai-connections` 所有者校验路径，不受影响。
- [x] 新增 `/system/capabilities`（鉴权）：只下发角色能力、上传限制与 Sheets 可用性，不含任何 Key 或平台敏感配置；已有测试断言响应体不出现 secret/api_key/webapp_url/password/token。

### 12.2 发布执行记录（2026-09-07）

- [x] 分支 `feat/workbench-v2-redesign` 经 [PR #4](https://github.com/liuzhne/paper-grading-system/pull/4) 合入 `main / 4e8835d`。
- [x] `pgs-production-gates` [run 34121822443](https://github.com/liuzhne/paper-grading-system/actions/runs/34121822443) 全部通过：`unit-and-sqlite-migrations`、`frontend-workbench`、`postgres-migrations-constraints-and-restore`、`docker-compose-smoke`、`deploy-vercel-production` 均 success。
- [x] 新增的 `frontend-workbench` job 首次在 CI 环境运行即通过，含从源码重建 `public/` 的漂移拦截；Vite 产物在 Linux/Node 22 与本机 macOS/Node 26 之间哈希一致，门禁未误报。
- [x] Production deployment `https://paper-grading-system-aa8zhb3rr-derrick-s-projects5.vercel.app`，已 alias 到 `https://paper-grading-system-kohl.vercel.app/`。
- [x] 线上冒烟：`/`、`/api/system/integrations`、`/workbench`、`/workbench/account`、`/workbench/tasks` 均 `200`；`/api/batches` `401`。
- [x] **`/workbench/assets/<不存在的文件>` 返回 `404 application/json` 而非 HTML**，`vercel.json` rewrite 中排除 `assets/` 的负向前瞻生效。这条若失效，浏览器会把 SPA 的 HTML 当脚本执行，故障表现为难以定位的语法错误。
- [x] 未认证访问收紧端点均返回 `401`：`/api/system/llm-check`（**此前无任何依赖，是公开端点**）、`/api/system/ops-readiness`、`/api/system/organization-readiness`、`/api/system/capabilities`、`/api/release-gates/profiles`。
- [x] 线上资源与仓库一致：`/workbench/assets/index-Dkj1ZNUj.js`、`index-DkSHFKNn.css`；旧 SPA 为 `app.3815a370efc3.js`，且线上产物中确认包含 `llmCheckButton.classList.toggle` 角色门控，回归修复已生效。
- [x] 备份：经用户明确授权跳过第 5 节备份恢复演练，理由为本次无 Alembic 迁移、无数据库结构变更与数据写入。此为一次性例外（先例见第 7 节），不改变第 5 节的常规要求。

**仍需人工完成：**

- [ ] 以真实非平台管理员账号登录生产，确认 `/api/system/ops-readiness`、`/api/system/llm-check`、`/api/release-gates/profiles` 返回 `403`（而非 401），且 `/api/system/organization-readiness` 返回本组织数据；确认旧 SPA 首页不再显示「测试模型连接」按钮。此项需生产凭据，未在本次自动冒烟中覆盖。
- [ ] 发布后观察窗口、值班责任人与常态化备份恢复演练。
- [ ] 发布责任人与批准人：`________________`　日期：`________________`

最终批准人：`________________`　日期：`________________`

## 13. 2026-09-08 v2 工作台正式入口发布记录

用户决定：导出出口一律加角色限制、不允许应用内切换组织、旧 SPA 直接下线由新页顶上、其余自审风险留到后续处理。

### 13.1 本次的行为变更

- [x] **导出出口全量角色门控。** `write-sheet` 早有门控，四个 GET 出口没有——而带走数据的恰恰是 GET：批次 xlsx 含全体学生学号姓名与各项得分，HTML 报告与 JSON 含完整评语与证据引文。组织归属只回答「是不是本组织的数据」，不回答「这个人该不该把它导出去」。
- [x] **取消应用内组织切换。** 移除切换器与 `switchOrganization`；入口只要还在，「不允许切换」就只是界面上的约定。隔离本身不变，仍由服务端执行。
- [x] **旧 SPA 下线。** 根路径直接给工作台，取消 `WORKBENCH_DEFAULT_ENTRY` 开关与常驻 `/legacy/`。新增邀请注册页与密码重置页：**邮件里已发出的链接指向 `/register?token=` 与 `/reset-password?token=`，收件人不会重新拿到新链接**，不接就等于把所有在途邀请作废。
- [x] 组装脚本改为以工作台为入口页。**只改 FastAPI 不够**——Vercel 直接静态托管 `public/`，`/` 命中的是 `public/index.html`，根本不经过 Python；后端改了而产物没改，本地与 Docker 都对，唯独生产还是旧页面。

### 13.2 首次推送被门禁拦下（无生产影响）

[run 34204970804](https://github.com/liuzhne/paper-grading-system/actions/runs/34204970804) 失败，`deploy-vercel-production` 为 `skipped`，**生产未发生任何变更**。两处失败的报错都指向了错误的方向：

- **PostgreSQL 门禁**报「lossy downgrade unexpectedly succeeded」，真正的缺陷隔了两步：`postgres_verifier.MIGRATION_SEQUENCE` 末项被当作预期 head 而未随 0024–0028 更新 → 它在建 fixture **之前**抛「unexpected alembic head」→ 后面那条「有数据时拒绝 lossy downgrade」拿到空库、守卫没数据可拒。既有的冻结契约测试没拦住：它拿自己的硬编码副本比对，两边同时过期就「一致」。
- **前端验收**报 `AUTH_PASSWORD_WEAK, AUTH_SECRET_WEAK`。鉴权模式验收本地一直是绿的，因为 `Settings` 的 `env_file` 含 `.env.local`，开发机上那份带的是**生产口令与生产 AUTH_SECRET**——本地验收一直在拿生产密钥当测试口令，只有在没有这个文件的机器上才暴露。与「测试打到生产库」是同一个根。

防复发做法见 RUNBOOK §11.4、§11.5。

### 13.3 发布执行记录

- [x] `pgs-production-gates` [run 34206548245](https://github.com/liuzhne/paper-grading-system/actions/runs/34206548245) 五个 job 全部 success，含 `deploy-vercel-production`。
- [x] 线上核验（`https://paper-grading-system-kohl.vercel.app`）：`/` 返回 `200` 且 `<title>有据智评</title>`、加载 `/workbench/assets/index-C2Gejaov.js`；`/login`、`/register`、`/reset-password` 均 `200`（已发出的邀请与重置链接仍可用）；`/legacy/` 返回 `404`（旧页确已下线）。
- [x] 发布边界：**无 Alembic 迁移**，head 保持 `0028_export_event_backfill`；未触碰 `services/scoring/*`、prompt 构造与 `cache/llm_cache.PROMPT_VERSION`，第 6 节的 QWK 要求不适用；`SCORING_ENGINE_MODE=legacy` 不变。

**仍需人工完成：**

- [ ] 以真实非平台管理员账号登录生产，确认四个导出出口对无 `org_admin` / `teacher` 角色的成员返回 `403`。此项需生产凭据，未在自动冒烟中覆盖。
- [ ] 用一封真实邀请邮件走通 `/register?token=` 全链路（自动验收用的是合成 token，只覆盖了页面可达与失效提示）。
- [ ] 发布责任人与批准人：`________________`　日期：`________________`


## 14. 发布记录：V3-0 / V3-1 / V3-3 / V3-4（2026-09-09）

### 14.1 发布边界

- **有 Alembic 迁移**：`0029_runtime_access_for_v2_tables` → `0030_platform_llm_config`。
  与前两次「无迁移」不同，本次**必须先迁移再部署代码**，理由见 14.2。
- 未触碰 `services/scoring/*` 的判分逻辑、prompt 构造与 `cache/llm_cache.PROMPT_VERSION`，
  第 6 节的 QWK 要求不适用；`SCORING_ENGINE_MODE=legacy` 不变，未申请也未授予 GATE-03。
- **行为变更（需在发布前知情）**：受保护部署的模型来源从环境变量改为
  `platform_llm_config` 单例（D-028）。`LLM_PROVIDER` 与 `PLATFORM_MANAGED_LLM_ENABLED`
  在生产**不再授予任何模型**。

### 14.2 部署顺序不能反

新代码启动即读 `platform_llm_config`。**先部署代码后迁移**，那段时间里读表会失败，
`_platform_runtime()` 按设计不回落 Mock，于是全站 LLM 动作报「尚未配置平台模型」。

读该表的两处（`/system/capabilities`、`/system/platform-llm`）已对「表不存在」容错，
读不到即视为「没有平台模型」，因此顺序反了不会整站崩。**但这是防线不是许可**：
`capabilities` 是前端启动就读的，它 500 会让整个工作台起不来。

正确顺序：

1. `pgs-production-migrate` 工作流，`expected_head=0030_platform_llm_config`，
   先 `dry_run=true` 看清当前版本与行数，再 `dry_run=false`；
2. 确认工作流的「Verify with the runtime role」通过——它用 `pgs_app` 复验，
   owner 什么都读得到，证明不了应用能不能工作；
3. 再合并到 main，由 CI 触发 Vercel 部署。

### 14.3 上线后必然出现的状态（这是期望行为）

迁移与部署完成后，生产进入「平台未配置模型」：

- 所有用户登录后被引导到「账户与连接」；
- 平台管理员可在「运维与质量 → 平台默认模型」配置，配好后全体恢复正常。

**这不是故障**（D-027/D-028）。此前的行为才是问题：`LLM_PROVIDER=mock` 时，没绑连接
的评分会**悄悄返回 Mock 假分**，界面上没有任何提示。

发布前需二选一（用户已知会，见 v3 计划 §8）：

- [ ] 迁移完成后**立即**由平台管理员配置真实模型，缩短窗口；
- [ ] 或接受一段「必须自带 BYOK」的窗口。

### 14.4 备份

- [ ] 本次**有** schema 变更（新增 `platform_llm_config` 表），不适用前两次的跳过例外。
      按第 5 节要求，迁移前完成备份并 verify。`0030` 在有配置时拒绝降级——那一行含密钥
      材料与「谁配的」，删掉要管理员重新找回 API key。

### 14.5 门禁结果（本地）

- 后端 `1895 passed`；前端组件/状态 `92 passed`；浏览器验收 `73 passed`（含 `auth` 与
  `llm-setup` 两个独立后端）。
- `typecheck` 通过；OpenAPI 合同无差异；`public/` 产物无漂移。
- 本机 SQLite 通过**不能**替代 CI 的 PostgreSQL 16 artifact，以 CI 结果为准。


### 14.6 执行记录（2026-09-09）

- [x] 代码部署：`4683d76`，`pgs-production-gates` 五个 job 全绿，
      `deploy-vercel-production: success`。
- [x] 数据库迁移：`pgs-production-migrate` run 34349237089，
      `0029_runtime_access_for_v2_tables → 0030_platform_llm_config`，
      dry-run 先行（当前 head 与 owner 身份均核对），正式执行成功。
- [x] 运行角色复验通过。
- [x] 线上核验：`/`、`/login`、`/api/system/integrations` 均 200，标题「有据智评」。

**部署顺序与计划相反**（先代码后迁移），未造成故障：读 `platform_llm_config` 的两处
已对「表不存在」容错（见 `ce7a0bd`）。这条容错是推送后复查发布顺序时补的，**不是
原设计**——RUNBOOK §11.11 仍要求先迁移。

**当前状态：平台模型未配置。** 所有用户登录后会被引导到「账户与连接」。这是期望
行为（D-027/D-028）。恢复正常使用有两条路，任选其一：

- [ ] 平台管理员在「运维与质量 → 平台默认模型」配置真实模型，全体恢复；
- [ ] 各用户自行在「账户与连接」绑定 BYOK 连接。

发布后发现并已修复的一项：`pgs-production-migrate` 的运行角色验证只覆盖了
`review_command_receipts` / `export_events`，漏掉 0023 的两张表与新建的
`platform_llm_config`。清单停在旧表上等于只证明了「以前那两张还能读」。已补全并
加测试，从迁移源码反推应验证的表。

## 15. 2026-09-11 平台模型配置折叠发布

- [x] 用户明确授权发布到生产；范围为 OpsView 配置折叠和对应静态产物、三文档。
- [x] 本地前端 171 项测试、类型检查与产物重建通过。
- [x] 无后端、数据库迁移、评分或 prompt 变更；head 保持 `0030_platform_llm_config`，默认 legacy 不变。
- [x] 发布提交 `b61797a5e7469149207bfeb15dc9b32d249e79f5`；[CI run 34546230807](https://github.com/liuzhne/paper-grading-system/actions/runs/34546230807) 的后端全量、前端浏览器验收、Postgres 迁移/恢复、Docker 冒烟及 production 部署全部成功。
- [x] 正式域名 `https://paper-grading-system-kohl.vercel.app` 的 `/`、`/workbench/ops`、`/api/system/integrations` 返回 200；入口引用 `index-v8hxlx6P.js`，线上 `OpsView-Dwdrl2o5.js` 与本地发布产物字节一致。未使用生产管理员身份执行保存或停用。
- 回滚：撤销本次提交并经同一 CI 发布；不执行数据库降级。

## 16. 2026-09-13 评分标准条款确认与原子编辑（已部署，待登录验收）

- [x] 用户授权实施、必要回归通过后生产发布；补充选择本次完成原子规则编辑。
- [x] 用户允许新增审核接口复用现有字段，不新增空 Alembic 迁移；head `0030_platform_llm_config` 不变。
- [x] 条款确认即时保存，批量限当前评分项，部分失败停止，确认不自动发布。
- [x] 原子规则、档位、来源随新草稿保留；不通过评分项投影合并原子规则。
- [x] 2026-09-13 本地后端 1911 passed（含迁移）；前端单元 176 passed、类型/接口快照通过；浏览器 139 passed、3 skipped；public 已重建并检查页面截图。
- [x] [PR #5](https://github.com/liuzhne/paper-grading-system/pull/5) 已合并；提交 `95d189f95ed079d2f90787965859441ab5ba4e0e` 的 [main CI 34746064881](https://github.com/liuzhne/paper-grading-system/actions/runs/34746064881) 后端、前端、Postgres 迁移恢复、Docker 冒烟及 production 部署成功。
- [x] 线上健康、首页和评分标准深链接 200；审核接口未登录返回 401。入口 `index-BQnCK4in.js`、`RubricsView-DSk2UsPD.js` 及相关 CSS 与本地发布产物字节一致。
- [ ] 登录后合成模板导入确认流程复验；当前页面仍在登录页，未执行生产写入。
- 回滚：恢复此前部署或撤销本次提交，经原 CI 发布；保留确认审计，不降级数据库。

## 2026-09-14 OpenRouter 规则起草修复（已发布并验收）

- 最终代码：`f350092d7c2711c2958aef54ce3c02990cb5233c`；[main 完整门禁与生产部署](https://github.com/liuzhne/paper-grading-system/actions/runs/34819189045) 全部通过。后端 1934 项测试通过，前端单元 178 项、类型/API 合同、浏览器验收、PostgreSQL 迁移/约束/恢复及 Docker 冒烟均通过。
- 最终生产健康与产物检查 8/8：首页、评分标准页、系统状态正常；未登录审核工作区保持 401；线上入口及评分标准 JS/CSS 与提交产物逐字节一致。
- 当前 Chrome 使用原 `OpenRouter-D · openrouter/free`，6 个评分项全部生成成功，共 40 条待确认建议（T01=9、T02=1、T03=12、T04=6、T05=9、T06=3），已逐项打开确认面板核对可见性。没有确认、发布或修改原模板条款。建议保留在当前页面；刷新会丢失尚未应用的建议。
- 真实生成链路在 `f453834` 实测，六个请求均为 HTTP 200，首个请求 `wxvs2-1789371607804-92a1ae19ab3f`，最后请求 `7zc6q-1789371684978-a6eaaa27ea58`。之后 `f350092` 仅规范化失败响应的错误字段，未改成功生成逻辑；为保留用户待确认建议，未刷新或重生成。

已确认的问题和最终处理：

| 生产证据 | 处理 |
|---|---|
| Chrome 422 响应为 MUTEX_GROUP_MISSING；上游已返回 200 | OpenRouter 起草声明严格 JSON Schema，补齐必填字段指令，仍严格验证来源、互斥和分值；仅允许一次结构纠正。 |
| 安全日志明确 output_truncated，连接公开 provider_options 为空 | 起草独立默认 8192 token，下限不覆盖连接显式 max_tokens，正式评分预算不变。 |
| Vercel 日志明确 Task timed out after 60 seconds（504） | Python 函数 maxDuration=300；未显式启用的额外 reasoning 在 OpenRouter 起草中关闭；单次起草不叠加传输重试。 |
| 上游 400 后出现 TypeError；数字 error.code=400 可本地重现 | ProviderError 规范化标量错误字段，忽略嵌套非标量字段，避免错误处理自身变成 503。 |
| 客户端忽略结构化错误正文，批量失败丢失已有结果 | 显示具体原因与操作建议；逐评分项生成、保留成功建议、重试只补剩余项。 |

历史首个 503 的日志没有安全解析原因，不能追认其确切异常；上述结论分别基于后续真实复现和本地协议回归。修复不放宽审核/发布权限、不记录模型正文或密钥、不修改数据库结构，迁移 head 仍为 `0030_platform_llm_config`。三份维护文档已同步。回滚按 RUNBOOK 撤销相应代码/配置提交并通过同一 main 门禁发布；无数据库降级。

## 2026-09-15 AI 规则组上限冲突修复（已部署并完成生产数据验收）

- 用户已授权业务代码修复、生产草稿数据调整及部署。范围：组上限与单条重复上限分离、旧客户端兼容、中文阻断提示及原子编辑防错；不修改 Core 计分算法、分值、互斥关系或数据库结构，迁移 head 仍为 0030_platform_llm_config。
- 本地后端全套 1947 passed；前端单元 179 passed、类型检查通过。增加存量已审核冲突在新草稿修复、旧版本/来源/审核记录保留、一次扣分遇到多次证据不重复扣分、非 AI 上限不清除、组配置非法拒绝等回归。评分标准浏览器回归 6/6 通过（含生成应用、确认、显式旧上限清除、新草稿保存、审核发布冻结）。完整 [main CI 34913724818](https://github.com/liuzhne/paper-grading-system/actions/runs/34913724818) 已通过并部署 e0dcb184931e0adeda28d29494fd81aa3df169cd；CI 后端 1947 passed、前端单元 179 passed、浏览器 140 passed / 3 conditional skipped、Postgres 迁移/恢复、Docker 冒烟全部通过。
- 生产修复对象为当前用户已指出的草稿中 T01 9 条、T02 3 条、T03 3 条 AI 规则；执行前重新核对，仅清除单条 cap_points 并保留组上限及所有评分条件；新草稿重确认此前审核条款，不自动审核其它原文或发布模板。
- 发布只走 main 全部门禁后的 Vercel 部署；回滚沿 RUNBOOK 撤销代码并重建静态产物，不删除历史草稿或审核日志，不做数据库降级。

- 生产部署后健康/产物验证 8/8 通过：入口、系统状态、未登录工作区 401，以及线上评分标准 JS/CSS 与本地构建逐字节一致。Chrome 刷新后曾因会话失效返回登录页，用户重新登录后继续执行。修复前只读记录 21 条规则（17 已确认、4 待确认），其中 15 条存在本次上限冲突。

- 生产数据已完成：通过原子编辑将 T01 9 条、T02 3 条、T03 3 条 AI 规则的单条累计上限由 20 清空；保存后生成 `v1.0-draft.5`，前序为 `v1.0-draft.4`。后端为上述 15 条保留 group_cap_points=20，分值、一次命中、正文、证据参数、互斥关系及其它编辑字段与修复前一致。
- 保存前、保存后及刷新后均比较全部 21 条规则编辑字段；唯一 15 项差异均为上述单条上限。刷新后 15 个校验阻断全部消除（页面 alert=0），组上限 15/15 可见且为 20。重新核对并确认此前已审核的 17 条（15 条 AI + 2 条原文），最终审核状态与修复前逐条一致：17 已确认、4 待确认。没有确认原先待确认的 4 条原文，也未提交审核或发布模板。
- 业务代码与生产数据修复均已验收完成；保留旧草稿及其审核历史，三文档同步。文档验收记录为后续 docs-only 提交，运行时代码仍为上述 e0dcb18。

## 2026-09-15 发布流程引导调整（已部署并验收）

- 第 3 步集中呈现发布前检查、提交模板审核、核对版本/范围并发布以及发布后冻结状态；前两步页头提供前往入口，复用现有视觉规范。
- 未更改后端接口、数据库结构、权限或评分规则；不为界面验收修改真实模板的审核/发布状态。
- 本地类型检查、179 项前端测试、6 条评分标准浏览器回归及 7 项文档契约通过；核对合成模板检查就绪/审核发布阶段截图，延续卡片、配色与表单格式。覆盖待办导航、全部完成、提交审核失败后重试、显式发布及发布后冻结。运行时代码 995da3aa61170cf8b24f301beca12f6f202b09de 经 [完整 main CI 34919369965](https://github.com/liuzhne/paper-grading-system/actions/runs/34919369965) 部署，全部门禁通过；回滚撤销本次前端及重建产物，经 main 全部门禁部署。

- 线上健康/静态产物检查 8/8 通过。Chrome 已刷新到新页面并只读进入第 3 步：条款全部已确认、模板映射无需核对、规则校验通过；提示下一步提交模板审核，卡片按钮可用，发布按钮数量为 0。真实模板仍为 v1.0-draft.5 草稿，未执行审核或发布；用户原有 21 条确认结果保持不变。


### 2026-09-15 上传后解析衔接

- 修复前端直传归档后未执行解析的问题，保留原文件 ID 重试；无数据库或评分逻辑变化。
- 本地验证：181 项前端单测、6 项上传浏览器测试、7 项三文档合同测试通过；类型检查通过。main CI 与生产验证如下。
- 回滚通过 revert 本次业务提交并重新运行完整 CI；不删除已上传材料。

- 2026-09-15 · 上传后解析衔接生产验收：c213a23 经 main CI 34920902739 全部门禁部署成功，生产页面/资源校验 9/9；Chrome 恢复原草稿的三份已上传材料并完成解析，刷新后仍为 3 份解析正常、0 阻断，开始评分可用，未启动评分。
- CI 证据：https://github.com/liuzhne/paper-grading-system/actions/runs/34920902739 。真实材料仅通过现有解析接口继续处理，未重新上传、未手动修改数据库。


### 2026-09-15 平台用量外键与评分异常状态

平台用量保留评分记录，连接外键按 BYOK 来源投影；批评分异常回滚后记录异常终态。无迁移、prompt 或计分算法变更。本地全量 1952 项通过，追加平台 Core 评分落库回归后专项 5 项通过；发布门禁及线上验证如下。

- 2026-09-15 · 平台用量异常存量修复：生产日志确认原请求 HTTP 500 后，锁定唯一目标批次并校验 scoring、state_version=2、原更新时间及 0 条评分结果，经 finish_scoring 转为 scored_with_errors、版本 3；同事务写入 batch.failed_request_state_reconciled 审计。三份材料保留，未重新评分。

- 2026-09-15 · 平台用量与异常状态生产验收：eaf947f 经完整 CI 34922785204 部署成功，生产服务/资源检查 9/9；Chrome 刷新确认目标批次退出 scoring，显示 scored_with_errors，材料 3 份、有效评分结果 0，未重新调用 AI。平台模型真实再次评分未执行；通过合成 Core 评分落库及全量回归验证修复。
- CI：https://github.com/liuzhne/paper-grading-system/actions/runs/34922785204 。

### 2026-09-15 持久化后台评分与运行详情

- [x] 新建评分从整批同步请求切换为持久化 score job，默认并发 2；刷新或关闭浏览器不影响后台执行。
- [x] 新增“正在评分”聚合页和任务运行详情；任务列表状态与进度均可跳转，详情展示心跳、六类计数、逐材料状态、取消、失败重试与完成后查看结果。
- [x] worker 可领取 queued、恢复租约过期的 running/cancel_requested；item 状态与 job 汇总计数同事务更新，取消或最终失败后批次退出 scoring。
- [x] Vercel 同步 run 入口返回 409，runner token 不下发；页面与日志不展示论文正文或密钥。
- [x] 数据库结构不变，按用户已授权边界不新增空迁移；Alembic head 保持 `0030_platform_llm_config`。评分算法、prompt、`SCORING_ENGINE_MODE=legacy` 与 GATE-03 权限不变。
- [x] `render.yaml` 声明 `0.5c-512mb` / Singapore Docker worker、checksPass 自动发布和 300 秒优雅退出；生产密钥均为 `sync: false`，Mock fallback 显式关闭。
- [x] 本地门禁：后端 1958 passed；前端单元 184 passed、类型与 API 快照更新通过；浏览器 143 passed / 3 条件跳过；静态产物已从源码重建，`git diff --check` 与 compileall 通过。
- [ ] main CI、Vercel 发布、独立 worker 创建及生产心跳验收完成后补录运行编号与服务标识。
- 回滚：先暂停 worker，再撤销本次前端入口与 API 变更并经 main 门禁发布；保留 job、item、评分结果和审计数据，不执行数据库降级。

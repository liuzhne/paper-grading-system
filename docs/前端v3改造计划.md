# 前端 v3 改造计划（模板生成→使用、任务使用→查看结果）

> 当前 Alembic head `0030_platform_llm_config`。**V3-0a 的服务层与持久化层已实施**（见
> ARCHITECTURE §7.3、D-028）；其余章节仍为方案。
> 生产已切到 v2 工作台、旧 SPA 已下线（见 [前端v2改造计划.md](前端v2改造计划.md) §0 决策 15）。

---

## 0. 本轮决策

| # | 决策 | 影响 |
|---|---|---|
| **V3-a** | **不保留空白新建**，评分标准只能由导入产生 | `POST /rubrics` 退出界面（CLI/测试仍用）。副作用是好的：每份标准都带模板溯源，`publish` 那条「legacy draft 必须先显式升级溯源」的分支在新界面上不再可能触发 |
| **V3-b** | **AI 起草必须用当前用户已写入的 API key 连接**，并要有配置入口 | 配置入口已存在（账户与连接页，功能完整）。真正要做的是：起草时**强制选连接、禁止回落**，无连接时挡住并引导去配置 |
| **V3-c** | **导入的默认可见范围为「仅自己可见」** | 与后端默认一致（`visibility` 默认 `private`），无需改后端 |
| **V3-d** | **发布时增加一步「分享范围」**：仅自己 / 本组织 / 所有人 | **新能力**。可见范围目前完全不可改：`PATCH /rubrics` 拒绝非草稿状态，且 `RubricUpdate` 里根本没有 `visibility` 字段。要在 `publish` 请求里加范围并原子生效 |
| **V3-e** | **平台默认模型由管理员在页面配置；系统初始化时没有平台默认模型** | **新能力**。平台模型现在只来自环境变量，没有任何配置表；`ai_connections` 的 `organization_id`/`owner_id` 都是 NOT NULL，装不下平台级连接，需要独立的单例配置表 |

---

## 1. 结论：先解决一个比「缺按钮」严重得多的问题

### 1.1 生产当前评出来的是 Mock 假分

`/api/system/integrations` 在生产返回：

```json
{"provider": "mock", "model": "mock", "adapter": "MockLLMScorer",
 "api_key_configured": false, "fallback_to_mock": true}
```

评分取模型的逻辑是（`backend/app/services/scoring/engine.py:147`）：

```python
if batch.ai_connection_id is None:
    return get_llm_scorer()          # → provider="mock" → MockLLMScorer()
```

而**新建任务页从头到尾没有让用户选择 AI 连接**——`NewTaskView.vue` 里没有任何
`ai_connection` 字段。于是通过当前界面建的每一个任务都 `ai_connection_id = null`，
**评出来的分数来自 Mock**，界面上没有任何地方提示这一点。

同一条路径也影响 AI 起草扣分细则：不带 `ai_connection_id` 调用
`draft-deduction-rules`，得到的是 Mock 编出来的扣分规则，却以「AI 起草 · 待确认」
的形态呈现给用户确认——**确认之后它们会进入正式发布的评分标准**。

决策 V3-b 正好堵住这条路，但覆盖面要扩大到**评分本身**，不止起草。
决策 V3-e 则把「平台有没有模型」从环境变量挪到管理员的显式配置上——**初始状态是
没有**，于是「不绑连接就评分」这条路从一开始就不存在，而不是靠某个 env 恰好设对。

> 注：`get_llm_scorer()` 里那道「认证部署禁止平台模型」的保护
> （`factory.py:57`）排在 `provider == "mock"` 分支**之后**，所以 provider 为 mock 时
> 它不生效。这不是配置写错，是保护顺序的问题。

### 1.2 四条链路断在半途

| # | 断点 | 现状 |
|---|---|---|
| **A** | **评分标准的全部创作能力随旧 SPA 一起消失** | 生产上无法新建、导入、审核或发布任何评分标准。后端 20 个端点齐全，新工作台只调用了 2 个只读端点 |
| **B** | 新建评分任务页无入口 | `/workbench/tasks/new` 路由与页面都在，全站没有任何链接指向它 |
| **C** | 评分工作区只读 | 不能改分、不能加评语、不能「确认此份」。设计稿的三个核心动作全缺 |
| **D** | 阻塞任务无法解决 | 队列能看到阻塞项，没有 claim/resolve 入口；阻塞项不清零就点不动「完成复核」——**provider 一出错，这个批次永远走不完** |

A 意味着系统当前无法完成一次从零开始的评分：新建任务要选「已发布的评分标准」，
而现在没有任何途径产生一个。

---

## 2. 事实核对：能力矩阵

后端能力**基本齐备**，v3 绝大部分是前端工作。下表逐行核对过路由文件。

### 2.1 评分标准（`backend/app/api/routes/rubrics.py`）

| 能力 | 后端端点 | 旧 SPA | 新工作台 | V3 |
|---|---|---|---|---|
| 列表 / 规则覆盖度 | `GET /rubrics`、`.../rule-coverage` | ✅/— | ✅ | 保留 |
| 新建（空白） | `POST /rubrics` | ✅ | ❌ | **按 V3-a 不做界面** |
| **导入 Excel + Word 批注** | `POST /rubrics/import-files` | ✅ | ❌ | **做** |
| **AI 起草缺失扣分细则** | `POST .../draft-deduction-rules` | ✅ | ❌ | **做（强制绑连接）** |
| 执行草稿（编译产物/歧义） | `GET .../execution-draft` | ✅ | ❌ | 做 |
| 逐条规则审核 | `POST .../rules/{code}/{submit-review\|approve\|reject\|reopen}`、`PATCH .../rules/{code}` | ✅ | ❌ | 做 |
| 模板链接审核 | `POST .../template-links/{id}/review` | ✅ | ❌ | 做 |
| 提交审核 / 退回草稿 / 重新编译 | `.../submit-review`、`.../return-to-draft`、`.../recompile` | ✅ | ❌ | 做 |
| **发布** | `POST .../publish`（需显式 `compilation_id`） | ✅ | ❌ | **做** |
| 克隆 | `POST .../clone` | ✅ | ❌ | 做（替代空白新建） |
| 编辑基本信息/评分项 | `PATCH /rubrics/{id}` | ✅ | ❌ | 做 |

### 2.2 评分任务与结果

| 能力 | 后端端点 | 旧 SPA | 新工作台 | V3 |
|---|---|---|---|---|
| 建批次 / 上传 / 预检 / 起评 | `POST /batches`、直传三步、`.../upload-precheck`、`.../score` | ✅ | ✅（**页面不可达**） | 补入口 |
| **选择 AI 连接** | `POST /batches` 的 `ai_connection_id` | ✅ | ❌ | **做（必选）** |
| 进度 / 概览 / 分布 | `.../progress`、`/overview`、`.../score-distribution` | — | ✅ | 保留 |
| 冻结正文与证据 / 相邻导航 | `.../document-view`、`items?include_view=true`、`/papers/{id}/neighbors` | — | ✅ | 保留 |
| **改单项分** | `PATCH /score-items/{item_id}` | ✅ | ❌ | **做** |
| **提交单份复核** | `POST /scoring-runs/{id}/review` | ✅ | ❌ | **做** |
| 复核记录 | `GET /scoring-runs/{id}/review-logs` | — | ❌ | 做 |
| 队列 / 批量采纳 / 完成复核 | `.../review-queue[/accept]`、`.../complete-review` | — | ✅ | 保留 |
| **阻塞任务 claim/release/resolve** | `/manual-review-tasks/{id}/...` | — | ❌ | **做** |
| 单份重评 / 重新解析 | `POST /scoring-runs/{id}/retry`、`POST /papers/{id}/parse` | ✅ | ❌ | 做 |
| 批任务取消 / 重试失败项 | `/batch-scoring-jobs/{id}/cancel\|retry` | ✅ | ❌ | 做 |
| 导出全链路 | `.../export-*` | 部分 | ✅ | 保留 |

**v3 需要新增的后端端点接近于零**；真正的后端工作是 §4 的模型绑定、权限与并发。

---

## 3. 目标：两条必须走得通的完整流程

### 3.1 流程一 · 模板标准：从生成到可用于评分

```
导入规则 Excel（可带 Word 批注模板）
  → 解析与编译 → 补全扣分细则（用自己的连接）→ 逐条确认
  → 提交审核 → 校验 → 选择编译产物发布 → 可被任务选用
```

每一步的**退出条件**（不满足不能进下一步，UI 必须说清为什么）：

1. **导入**：必须上传规则 Excel。返回的 `warnings` 与 `template_summary` **原样展示**，
   不吞掉。可见范围默认「仅自己可见」（V3-c）。
2. **解析与编译**：产生 `RubricCompilation`。有歧义（`ambiguity`）时明确标出，不替用户挑。
3. **补全扣分细则**：`rule-coverage` 给出每项的 `complete / pending_review / missing`。
   `missing` 是**阻断项**，UI 要说清后果：「评分到该项时没有判据可用」。
   起草**必须选一个自己的 AI 连接**；没有连接就挡住并链到账户与连接页。
   AI 只补缺失部分，**用户原文表述保留**。
4. **逐条确认**：AI 起草的规则默认 `待确认`，**未确认不进入可执行版本**。确认记录
   确认人、时间、模型与生成指纹（后端已有）。
5. **提交审核 → 校验**：有阻断项时拒绝提交，给出「去修改 T02 →」式定位跳转。
6. **发布（两件事一次做完，V3-d）**：
   - 选择**编译产物**：要求显式 `compilation_id`——UI 必须让用户看到自己发布的是哪
     一份，不提供「用最新的」快捷方式；
   - 选择**分享范围**：仅自己 / 本组织 / 所有人。默认沿用导入时的「仅自己」，
     改大范围是用户的显式动作。
   两者在同一个请求里原子生效。发布后版本冻结。
7. **可用于评分**：新建任务页的标准下拉只列已发布**且当前用户可见**的版本。

### 3.2 流程二 · 评分任务：从使用到查看结果

```
新建任务 → 选已发布标准 → 选自己的 AI 连接 → 上传材料 → 解析预检
  → 开始评分 → 监控与异常处置 → 逐份复核（改分/评语/确认）
  → 解决阻塞项 → 完成复核 → 导出
```

关键退出条件：

1. **新建任务**：工作台与评分任务页都要有入口（现在两处都没有）。
2. **选 AI 连接**：**必选**。不选就不能开始评分——否则评的是 Mock（§1.1）。
3. **上传与预检**：已实现。预检问题如实展示。
4. **开始评分**：批次进入 `scoring`，逐份检查点，中断可续跑。
5. **异常处置**：失败材料可单独重试，整批可取消。**目前完全缺失**。
6. **逐份复核**：能改分、写理由、加评语、「确认并进入下一份」。改分 bump
   `review_revision`，并发冲突原子拒绝。
7. **解决阻塞项**：能 claim → 处理 → resolve。**不解决就完不成批次，必须有。**
8. **完成复核 → 导出**：已实现。

---

## 4. 后端改动清单（少，但都不能省）

### 4.0 平台默认模型：从环境变量搬到管理员配置（V3-e）

现状：平台模型完全由 `LLM_PROVIDER` / API key 等环境变量决定，没有任何配置表，也就
没有「谁配的、什么时候配的、配了什么」的记录。`PLATFORM_MANAGED_LLM_ENABLED` 只是
一个 env 开关。

**不能复用 `ai_connections`**：它的 `organization_id` 与 `owner_id` 都是 NOT NULL
（`0021_private_ai_connections.py`），且有 `CheckConstraint("scope = 'private'")`。
放平台连接进去要把两个必填列改成可空并放宽约束——**为一个单例记录削弱一张多租户表
的完整性约束，代价太大**。

改为**独立的单例配置表** `platform_llm_config`：

- 字段沿用 BYOK 那套信封加密（`api_key_ciphertext` / `nonce` / `tag` / `key_version` /
  `key_last4`），**不新造一套密钥存储**；
- 记录 `configured_by` / `configured_at`，让「谁把平台模型换了」可追溯；
- 带 `enabled` 开关，出问题时管理员可一键停用而不必删配置。

端点（**限平台管理员**）：`GET`（脱敏读）、`PUT`（写入或替换）、`POST /test`（试连）、
`POST /disable`。

`get_llm_scorer()` 的解析顺序改为：

```
绑定的 BYOK runtime  →  平台配置表（存在且 enabled）  →  抛错（不回落 Mock）
```

环境变量退化为**开发与 CI 专用**：`AUTH_ENABLED=false` 时仍走 env（否则本地开发与
全套验收会立刻断掉）；认证部署一律以配置表为准。

**初始状态是「未配置」**，因此一个全新部署在管理员介入之前没有平台模型——所有 LLM
动作都必须绑 BYOK 连接，且失败信息是明确的，不是一份 Mock 结果。

### 4.1 最高优先：让「没有可用模型」变成明确失败，而不是假结果

三处一起改，缺一处就还能绕过去：

1. **`get_llm_scorer()` 的保护顺序**：把「认证部署禁止平台模型」的判断提到
   `provider == "mock"` 分支**之前**。认证部署下没有绑定连接就调用，应当抛错，
   而不是安静地返回 `MockLLMScorer`。
2. **`POST /batches` 要求 `ai_connection_id`**：认证部署下缺失即 422，错误文案指向
   账户与连接页。（开发模式 `AUTH_ENABLED=false` 保持可空，否则本地与验收全断。）
3. **`draft-deduction-rules` 同样要求连接**：现在 `ai_connection_id` 可空且回落到
   `get_llm_scorer()`。

> 这三条是 V3-b 的实际落地。**不做这一步，后面所有界面工作都建立在假分数上。**

### 4.2 13 个评分标准写端点没有角色门控

`rubrics.py` 里只有 `create_rubric` 与 `import_rubric_from_files` 调了
`require_organization_role`，其余 **13 个写端点只做组织可见性检查**：

```
clone_rubric              draft_rubric_deduction_rules   submit_rubric_review
return_rubric_to_draft    recompile_rubric_draft         submit_atomic_rule_review
patch_atomic_rule         approve_atomic_rule_review     reject_atomic_rule_review
reopen_atomic_rule_review review_atomic_rule_template_link
update_rubric             publish_rubric
```

**发布一个评分标准决定了全组织的论文怎么被打分。** 它们现在难以触及只是因为 UI 没
了；v3 恰恰要把它们放到界面上，**必须先补门控再暴露**。做法与导出出口那次一致，
并补一条覆盖全文件写方法的测试。

### 4.3 编辑并发（已复核：不需要新迁移）

`PATCH /rubrics/{id}` 与逐条规则编辑没有乐观并发标识，两人同编同一草稿会后写覆盖
先写。复用批次那套：给 `Rubric` 加 `state_version`，写入校验，冲突 409。
~~这是 v3 唯一需要的新迁移。~~ **复核结论：不需要。** `recompile` 已要求显式
`supersedes_compilation_id`，基于非活跃草稿提交会被整体拒绝；`PATCH` 在创建即产生编译
产物的前提下基本不可达。详见 D-031。

### 4.4 可能需要的聚合读端点

详情页三步要同时用 `rubric` + `execution-draft` + `rule-coverage` + 规则明细。先用现
有端点并行取；**只有实测出 N+1 或首屏明显变慢才加聚合端点**，不提前造。

### 4.5 发布时选择分享范围（V3-d）

可见范围**目前完全不可改**：`PATCH /rubrics/{id}` 在 `status != "draft"` 时返回 409，
而 `RubricUpdate` 里根本没有 `visibility` 字段。所以这是新能力，不是接线。

做法：`RubricPublishRequest` 增加 `visibility`，与 `compilation_id` 在同一次
`publish` 里原子生效。

权限（**这会放宽一条现有规则，需要明确知情**）：

| 范围 | 现在（导入时） | V3-d（发布时） |
|---|---|---|
| 仅自己 | 任何人 | 任何人 |
| 本组织 | **需 `org_admin`** | **创建者本人即可** |
| 所有人（跨组织） | 需 `platform_admin` | 维持 `platform_admin`；其他人看到该项禁用并附说明 |

放宽「本组织」的理由：决策 V3-c 让导入默认仅自己可见，若共享仍需管理员，等于每份
标准都要走一次审批才能给同事用。跨组织的「所有人」影响面不同，维持平台管理员。

**发布后不可再改范围**（与「已发布模板请先复制为新版本后再编辑」一致）。想扩大范围
的路径是 `克隆 → 新版本 → 发布时选更大范围`。这条要在 UI 上说清楚，否则用户会以为
自己被永久锁死。

### 4.6 不需要改的

评分任务侧的写能力（改分、单份复核、重试、取消、阻塞任务处置）后端全部就绪，且已
有并发与幂等约束。这一侧是纯前端接入。

---

## 5. 页面与交互（对齐设计稿）

### 5.1 评分标准：列表 + 三步详情

| 步骤 | 内容 | 主要动作 |
|---|---|---|
| **1 · 基本信息与评分项** | 名称、版本、总分、可见范围、评分项表 | 编辑保存 |
| **2 · 评分规则** | 完整度总览；按评分项展开规则组（问题类型/严重程度/扣分/触发条件/**来源**） | `生成全部缺失细则`（需选连接）、逐条 `确认`/`排除`、`确认并应用全部` |
| **3 · 校验与发布** | 阻断项清单 + 定位跳转；**编译产物选择**；可见范围 | `保存并重新校验`、`提交模板审核`、`发布` |

头部常驻状态条：`v3.2 草稿 / 有未保存的修改 · 当前校验结果来自上一个执行草稿`。
**这句话不是装饰**——它区分「你改了但还没重新编译」和「校验结果是新的」，缺了它
用户会把过期的校验结果当成当前状态。

列表页补 `导入评分模板` 与每行 `克隆`（**不做空白新建**，V3-a）。

### 5.2 AI 连接的配置与选择（V3-b）

配置页**已存在**且功能完整（账户与连接：新建、保存前试连、轮换密钥、停用）。
v3 只需补「选择」与「挡住」：

- 新建任务页加**必选**的连接下拉，显示 `名称 · provider · 模型`；
- 评分标准页的 `生成全部缺失细则` 同样要选；
- **没有任何可用连接时**：两处都禁用主按钮，给一句话说明 + 链到账户与连接页，
  不允许「先跑起来再说」；
- 连接的密钥轮换或配置变更后，已建批次会因快照不一致而拒绝继续
  （`engine.py:158`），UI 要把这条错误翻译成「该任务绑定的连接已变更，请重新建任务」。

### 5.3 平台模型配置：管理员页面（V3-e）

放进**运维与质量**页的一个新分区，**只对平台管理员可见**（该页已有平台/组织两级
角色区分，不必新开一页）。内容：

- 当前状态：`未配置` / `已配置 · <provider> · <model> · 末四位 ****abcd` / `已停用`；
- 表单：provider 类型、base_url、模型名、API key，以及**保存前试连**（复用账户与
  连接页那套 `test-draft` 的交互，不要让人先存错的再试）；
- `谁在什么时候配置的`；
- `停用`：保留配置但立即停止使用。

**未配置时**，全站与 LLM 相关的按钮给同一句话：「本部署尚未配置平台模型，请绑定你
自己的 AI 连接，或联系平台管理员配置」——并分别链到账户与连接页 / 说明。

### 5.4 发布：编译产物 + 分享范围（V3-d）

发布确认是一个两段式的确认框，不是一个按钮：

1. **发布哪一份**：列出可选编译产物及其摘要（规则条数、来源分布、生成时间），
   默认选中当前活动的那份，但**必须由用户确认**；
2. **分享给谁**：仅自己 / 本组织 / 所有人。默认沿用导入时的范围。
   无权限的选项禁用并附一句为什么。

确认框底部提示：**发布后版本与范围都会冻结**，扩大范围需克隆为新版本。

### 5.5 评分任务：补入口与工作区写能力

- **工作台**与**评分任务页**都加 `新建评分任务` 主按钮（设计稿里出现 6 次，全站最高频）。
- **评分工作区**从只读改为可写：每项可改分（带理由）、`添加评语`、
  `确认此份评分` / `确认并进入下一份`；显示 `review_revision`，冲突提示刷新而非静默覆盖。
- **任务行**补 `重新解析`（单份）、`重试失败项`、`取消`（整批）。
- **复核队列**的阻塞项补 `领取 → 处理 → 解决`，解决后触发重算。

---

## 6. 阶段划分与退出条件

每阶段**先写测试再写实现**，组件/状态测试与浏览器验收同步补。

| 阶段 | 内容 | 退出条件 |
|---|---|---|
| **V3-0a** | §4.0 平台模型配置表 + 管理员页面 + `get_llm_scorer()` 解析顺序改造 | 全新部署「未配置」状态下，任何 LLM 动作都明确失败而非返回 Mock；管理员配置后可用；`AUTH_ENABLED=false` 的本地与验收不受影响 |
| **V3-0b** | §4.1 模型绑定三处 + §4.2 十三个写端点门控 + §4.3 `state_version` 迁移 | 认证部署下不绑连接调用 LLM 直接失败；非授权角色被拒；并发编辑返回 409 |
| **V3-1** | 评分标准列表补 `导入` / `克隆`；导入结果如实展示 warnings 与 template_summary | 能从零导入一份标准并看到解析结果 |
| **V3-2** | 三步详情步骤 1、2（含 AI 起草与逐条确认，强制选连接） | 未确认规则不进入可执行版本；无连接时起草被挡住 |
| **V3-3** | 三步详情步骤 3：校验、阻断定位、§4.5 的发布（编译产物 + 分享范围） | **一份新标准可走完「导入→确认→发布并分享给组织」，同组织另一个账号能在新建任务下拉里看到它** |
| **V3-4** | 新建任务入口 + **必选 AI 连接**；批任务取消/重试/重新解析 | 从工作台点进去能建任务并跑完；不选连接不能开始 |
| **V3-5** | 工作区写能力：改分、评语、确认此份/下一份；`review_revision` 冲突处理 | 改分写入复核记录；并发冲突被拒而非覆盖 |
| **V3-6** | 阻塞任务 claim/release/resolve | **provider 失败的批次能被人工解决并完成复核** |

**V3 的完成定义**：在干净环境里完成
「配置连接 → 导入标准 → 确认规则 → 发布并分享给组织 → 新建任务 → 上传 → 评分 →
改一份分 → 解决一个阻塞项 → 完成复核 → 导出」，全程不碰 CLI、不碰数据库，**且评分
用的是真实模型而非 Mock**。

分享那一步要用**第二个账号**验证：同组织的另一名教师能在新建任务的下拉里选到这份
标准——只用创建者自己的账号验，等于没验分享。这条端到端路径进浏览器验收矩阵。

---

## 6.1 实施进度（2026-09-09）

每项都先写测试再写实现；三文档同步按 CLAUDE.md 的规则随改随补。

| 阶段 | 交付 | 状态 |
|---|---|---|
| V3-0a | `platform_llm_config` 单例 + 管理端点 + 运维页配置区 + 解析顺序改造 + 未配置时引导 BYOK | ✅ |
| V3-0b | 13 个评分标准写端点补角色门控；并发保护复核（D-031） | ✅ |
| V3-1 | 评分标准导入与克隆；导入结果如实展示 | ✅ |
| V3-3 | 发布时选编译产物与分享范围（D-029），前后端 | ✅ |
| V3-4 | 新建任务入口（两处）+ AI 连接绑定 | ✅ |
| V3-2 | AI 起草缺失细则（强制绑连接）；逐条确认沿用后端既有语义 | ✅ |
| V3-5 | 工作区写能力：改分（理由必填）、评语、确认此份/进入下一份 | ✅ |

后端 1896（起点 1841），前端组件/状态 92，浏览器验收 73，Alembic head
`0030_platform_llm_config`。

### 与方案的偏离

- **§4.3 的 `state_version` 未实施**。核实后发现 `recompile` 已有编译层的乐观并发，
  而 `PATCH /rubrics` 在「创建即产生编译产物」的前提下基本不可达。加一个没人读的列
  只会让后来者不知道该信哪套机制。详见 D-031。
- **V3-6 尚未实施**：阻塞任务 claim/release/resolve。这是下一段。

### 已可部署

V3-0/1/3/4 构成一个自洽的增量：**平台模型可配、标准可导入可发布、任务可新建并绑定
模型**。发布边界、部署顺序与上线后必然出现的「平台未配置」状态见
[上线清单 §14](上线清单.md)。

## 7. 风险与控制

| 风险 | 控制 |
|---|---|
| **界面工作建立在 Mock 假分之上** | §4.0 与 §4.1 必须最先做完，不与任何界面阶段并行 |
| **写端点搬上界面而门控没跟上** | §4.2 与 §4.1 同属 V3-0b，一起做完再开 V3-1 |
| **改造平台模型解析顺序把本地开发与 CI 全断掉** | `AUTH_ENABLED=false` 保持走环境变量；V3-0a 的退出条件里明确要求本地与 55 项浏览器验收不受影响 |
| **平台模型配置成了新的密钥泄露面** | 复用 BYOK 的信封加密与脱敏读；页面只回显末四位；配置与停用都写审计 |
| **发布后范围锁死，用户以为没救** | 确认框明说「扩大范围需克隆为新版本」，并在已发布详情页给克隆入口 |
| **放宽「本组织」共享后，教师互相看到彼此的草稿** | 放宽的只是**发布时**选择范围；草稿仍受 `private` 约束，且发布是显式动作 |
| AI 起草覆盖用户原文表述 | 后端已保证只补缺失；前端标出每条规则的 `来源`（原文/AI），保留「排除」 |
| 发布了错误的编译产物 | 发布必须显式选择 `compilation_id` 并展示其摘要，不提供「用最新的」 |
| 改分与批量采纳互相覆盖 | 共用 `review_revision`；批量采纳已拒绝改过分的项，单份改分同样校验 |
| 阻塞任务被两人同时处理 | 用后端已有的 claim/release 租约，UI 显示当前领取人 |
| 连接密钥轮换后旧任务不可续 | 后端已 fail-closed；UI 要把错误翻译成可操作的说明（§5.2） |

---

## 8. 未决事项（动工前需确认）

1. **生产的过渡窗口**：V3-0a 落地后，生产会进入「平台未配置模型」状态，此时既有的
   `LLM_PROVIDER=mock` 不再被采用。是先由管理员配好真实模型再发布这一版，还是接受
   一段「必须自带 BYOK」的窗口？**两者都可以，但要先决定，否则发布当天才发现没人能评分。**
2. **平台模型的用量归属**：平台模型被所有没绑 BYOK 的用户共用，费用记在平台账上。
   是否需要按组织记录用量（`ai_usage_ledger` 已有结构），以及是否设配额？首版可不做，
   但要知道现在不做就没有账。
3. **设计稿与已定决策冲突的三处文案**，实现时按后者改：
   - 设计稿提到扫描件走 OCR → 决策 1 明确**不支持扫描件**；
   - 设计稿写「拖入……或包含材料的压缩包」→ 决策 12 已降级为**多选文件**；
   - 设计稿写单份上限 60 MB → 配置实为 **50 MB**。

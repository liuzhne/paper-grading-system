# 通用评分内核改造计划（论文 Profile 迁移版）

> 刷新日期：2026-07-18
> 代码基线：49ee2d3（源分支 feat/v2-rubric-lifecycle）
> 实现分支：Convert2RubricScalePlatform
> M0 实现提交：bb5c3b9eb7f4656896ba7b30dfa19e13267e4955、e07d632a96b9ec0f64f7900999d9fb40074615fd
> 数据库迁移：0011_version_hash_on_update
> 测试基线：318 passed，5 warnings（本地全套复验）
> 产品定位：**模板驱动的通用评分内核 + 论文作为首个业务 Profile**

本文件替代 2026-06-18 版本的产品化计划。旧计划中的本地模型、离线 CLI、鉴权、部署等工作已经基本落地，不再作为当前主线；历史明细可从 Git 历史和执行进展文档查阅。

---

## 0. 执行摘要

### 0.1 当前判断

项目已经拥有两块有价值但尚未接通的资产：

1. 现有论文评分链已经拆成 collect → compute → persist，并支持无数据库计算，不需要推倒重写。
2. RubricCompilation、RubricVersion、AtomicRule、RuleLevel、来源追踪、发布冻结、稳定哈希和深克隆已经完成，但目前仍是评分主链之外的“旁路模型”。

当前最重要的任务不是继续增加论文功能，也不是立即重命名 Paper，而是完成一条可运行的纵向链路：

    已发布 RubricVersion
        → RuleExecutionPlan
        → CheckerRegistry
        → score_submission()
        → LegacyPaperAdapter
        → 现有 ScoringRun / ScoreItem

随后再把论文解析、检查器、提示词、报告和评估迁入 ThesisProfile，并用第二个非论文 Profile 验证边界。

### 0.2 总体路线

    稳定论文基线
        ↓
    修复 weight / evidence / cache / policy / deductive 授权
        ↓
    定义不可变 Snapshot、Policy、Outcome 合同
        ↓
    建立版本化执行计划与 CheckerRegistry
        ↓
    用最小纵向切片跑通 score_submission()
        ↓
    score_paper() 经 LegacyPaperAdapter 调用通用 Core
        ↓
    抽出 ThesisProfile，完成论文 parity（默认仍为 legacy）
        ↓
    增加通用 Submission 持久化、v2 API、run-export@2
        ↓
    用 technical_proposal Profile 做非论文端到端验收
        ↓
    M8 经发布门禁后切换论文默认运行链到 Core

### 0.3 本阶段明确不做

- 不先把旧论文系统全部做完再整体拆分。
- 不一次性重写评分算法；优先复用现有 compute_scoring() 的成熟逻辑。
- 不立即物理重命名 papers、paper_chunks 或删除旧 API。
- 不同时泛化静态 Web、Streamlit 和 CLI；先完成 Core、API/CLI 与两个 Profile。
- 不把 CheckerRegistry 做成外部可安装插件系统；首阶段只做仓库内显式注册。
- 不用第二个 Profile 的单个合成案例宣称评分准确；它只验证架构通用性。

---

## 1. 当前实现的事实基线

### 1.1 版本与质量状态

| 项目 | 当前事实 |
|---|---|
| 规划起点 | feat/v2-rubric-lifecycle / 49ee2d3 |
| 实施分支 | Convert2RubricScalePlatform |
| M0 实现提交 | bb5c3b9eb7f4656896ba7b30dfa19e13267e4955、e07d632a96b9ec0f64f7900999d9fb40074615fd |
| 最新迁移 | 0011 |
| 全套测试 | 318 passed，5 warnings |
| 默认模型 | Mock LLM，可离线、自包含测试 |
| 当前评分入口 | score_paper(db, paper_id) |
| 当前业务对象 | Paper / PaperChunk / GradingBatch |
| 当前规则执行对象 | Rubric / RubricCriterion |
| 新规则模型状态 | 已建模、已迁移、已测试，但未接生产导入/API/评分 |

### 1.2 可以直接复用的能力

- 现有评分编排已分为 collect_scoring_inputs()、compute_scoring()、persist_scoring()。
- CLI 的无数据库模式已经证明解析结果可直接进入计算层。
- deductive、banded、hybrid、llm_direct 的基础计分逻辑。
- 证据结构、人工复核、ReviewLog、报告和导出基础设施。
- LLM provider、Mock、重试、缓存账本、离线模式、本地模型支持。
- RubricCompilation、SourceArtifact、SourceRule、RubricVersion。
- AtomicRule、RuleLevel、TemplateItem、RuleTemplateLink。
- 发布状态机、签核、不可变哈希、发布冻结和深克隆。
- eval 的 QWK、MAE、RMSE 等数学函数及动态评分项展开能力。
- FastAPI、SQLAlchemy、Alembic、鉴权、存储和 CLI 基础设施。

### 1.3 最大的运行时断点

当前真实链仍然是：

    Paper
      → GradingBatch.rubric
      → Rubric.criteria
      → score_paper()
      → ScoringRun(rubric_id)

具体断点如下：

- 实际评分不读取 RubricVersion、AtomicRule 或 RuleLevel。
- ScoringInputs.rubric_version 实际只是旧 Rubric.version 字符串。
- checker_key / checker_params 不参与调度。
- workflow_profile / global_policy 不控制运行行为。
- ScoringRun 不记录 rubric_version_id、version_hash、policy 或 execution plan 身份。
- 新生命周期服务没有被当前 Rubric API 或 CLI publish 调用。
- 当前导入只持久化 Rubric + RubricCriterion，不生成完整来源图和原子规则图。
- 批次只绑定 rubric_id，且没有锁定不可变 RubricVersion。

### 1.4 当前论文耦合

| 层 | 论文耦合 |
|---|---|
| 数据模型 | Paper、PaperChunk、学号、学生、学院、专业、导师 |
| 解析结果 | ParsedPaper、摘要、目录、结论、参考文献 |
| 检查器 | 按“参考文献、字数、图表”等中文名称调度 |
| 规则 | 固定 3000 字、部分固定扣分比例 |
| 语义分析 | 无条件执行研究问题—结论等论文一致性分析 |
| LLM | 提示词写死“毕业论文评阅助手” |
| API | /papers、paper_id |
| 输出 | 论文 HTML / JSON / Excel 字段 |
| 界面 | Web、Streamlit、CLI 的论文文案和元数据 |
| 评估 | 论文数据集构建、固定百分制等级、QWK 流程 |

### 1.5 通用化前必须解决的正确性问题

1. **weight 被接受但未执行**
   API 和数据库允许 criterion.weight，运行时却只累加原始分数；人工改单项后的重算同样错误。

2. **证据可信边界不严**
   空白 quote 可绕过原文校验；伪造 quote 虽被标记失败仍可能留在 evidence 中；evidence_sufficient 仍信任模型自报。

3. **缓存键不是实际提示的完整身份**
   当前键遗漏目标标题、候选 location、section_title、部分运行参数和不可变 version hash。

4. **deductive 没有授权边界**
   模型返回的任意 points 会被直接累加，未校验 rule_ref、max/cap、repeat、mutex、dependency 或 evidence policy。

5. **global_policy 没有执行**
   等级、复核边界和舍入散落在硬编码与全局配置中；非 100 分标准会被固定百分制错误评级。

6. **界面和文档存在旧口径**
   部分 README/Web 文案仍描述已废弃的 80% 常规封顶，迁移号也停在 0008。

### 1.6 审计锚点

| 结论 | 当前代码位置 |
|---|---|
| 评分仍从 Paper 和旧 RubricCriterion 取输入 | backend/app/services/scoring/engine.py:91、124、340 |
| 总分忽略 weight，等级/复核固定 | backend/app/services/scoring/rules.py:4、21、39 |
| 空 quote 可绕过匹配 | backend/app/services/scoring/validator.py:58–70 |
| deductive 直接累加模型 points | backend/app/services/scoring/engine.py:560 |
| 中文名称调度、3000 字和比例硬编码 | backend/app/services/checkers/deterministic.py:26、34、74 |
| 缓存请求遗漏真实 prompt 字段 | backend/app/services/cache/llm_cache.py:24；LLM adapter 的 input payload |
| 新 provenance / AtomicRule 模型已存在 | backend/app/db/models.py:136、267、386 |
| ScoringRun 尚无版本身份 | backend/app/db/models.py:1248 |
| 新生命周期服务未接现有 API | backend/app/services/rubrics/lifecycle.py；backend/app/api/routes/rubrics.py |
| 导入仍只写 Rubric + RubricCriterion | backend/app/services/rubric_import/persist.py:18 |

---

## 2. 架构决策摘要

完整设计见 [通用评分内核架构方案](docs/通用评分内核架构方案.md)。本文件只保留影响排期和验收的稳定决策。

### 2.1 分层边界

- Core 负责 Snapshot、RuleExecutionPlan、AtomicRule 执行、ScoringPolicy、证据验证、缓存、审计和人工复核 outcome。
- ThesisProfile 负责论文元数据、结构、检查器、prompt、Mock、报告扩展和论文 QWK。
- TechnicalProposalProfile 负责技术方案元数据、结构、prompt 和报告扩展。
- LegacyPaperAdapter / LegacyRubricAdapter 负责旧表与旧标准兼容。
- API、CLI 和 Profile 都不能绕过 Core 的分值授权、证据验证和 policy 聚合。
- scoring/core 不得 import Paper、ParsedPaper、SQLAlchemy Session、FastAPI 或论文模块。

### 2.2 运行合同

| 合同 | 关键要求 |
|---|---|
| SubmissionSnapshot | profile_key、source hash、通用 metadata，不含 Paper ORM |
| DocumentSnapshot | content hash、sections、evidence units、metrics、profile extensions |
| CompiledRubricSnapshot | published version 或明确标记的 legacy snapshot |
| RuleExecutionPlan | 稳定规则拓扑、policy、checker manifest、plan hash |
| ScoringPolicy | 聚合、等级、舍入、证据、复核和失败策略 |
| ScoringOutcome | raw score、contribution、rule decisions、review issues、完整运行身份 |

### 2.3 已冻结的关键语义

| 主题 | 决策 |
|---|---|
| 无 weight | points 模式；各 max_score 之和等于 total_score |
| 有 weight | weighted_normalized；所有 weight 必须存在且大于 0；按 total_score × weight/权重和 × awarded/max 计算贡献 |
| 语义证据 | quote 非空、chunk 有效且逐字命中；evidence_sufficient 由系统推导 |
| 确定性证据 | 使用 DeterministicObservation，必须可从 snapshot 重放 |
| deductive | 模型只返回已授权 rule code / verdict；points 由 AtomicRule 决定 |
| banded | 模型只能选择已发布 RuleLevel |
| llm_direct | 仅 legacy_unversioned 可生成 LegacyDirectCriterionNode；正式版本发布前必须迁为 band/deduct/review-only |
| hybrid | legacy 适配为 composite；正式版本在发布时展开为独立叶子 criterion，再按普通规则执行 |
| checker | 仅显式 namespaced registry；禁止按中文名称或动态 import 调度 |
| cache | 直接 hash 实际 PromptEnvelope；profile/policy/version/checker/location 全入身份 |
| Profile | business_profile_key 表示 thesis/proposal；workflow_profile 保留导入/编译工作流语义 |
| 正式规则身份 | rubric_source_kind=published_version，必须有 version ID + signed hash |
| legacy 规则身份 | rubric_source_kind=legacy_unversioned，正式 version 字段为空，但必须有 rubric_snapshot_hash |
| hash 升级 | 现有发布算法冻结为 rubric-content-v1；snapshot/plan/policy 分别声明 scheme/version |
| 历史 run | 权威字段永不回填；任何推断关联只能作为非权威分析数据 |
| compare | legacy run 是唯一权威结果；Core candidate 写独立 comparison artifact |
| 人工复核 | RuleDecision 不可变；human override 单独审计；未解决 block/invalid 时 final_total 为空 |
| 幂等 | 新 Core run 持久化 idempotency_key + rescore_generation，由数据库唯一约束裁决唯一权威 run |
| 默认切换 | M5 只做 parity；M8 才把旧论文入口默认切到 Core |

### 2.4 数据演进摘要

- 0012 先持久化冻结 policy、aggregation 和 invalid/blocked 状态，保证 M1 的初评与跨请求复核使用同一口径。
- 0013 再给现有批次/运行补版本、plan、document、engine 与幂等身份，并冻结 content-addressed DocumentSnapshot。
- 0014 增加 evaluation_batches、submissions、document_snapshots 和 scoring_runs.submission_id。
- 0014 对 paper_id / submission_id 使用 exactly-one CHECK；任何新通用实体存在时 downgrade 必须明确拒绝。
- 不物理重命名 papers，不复制历史 Paper，不伪造 RubricVersion。

---

## 3. 分阶段执行计划

### 3.1 总览

以下为单名后端工程师的乐观净实现量，不含等待真实标注、正式发布观察期和大规模前端重构。

| 里程碑 | 状态 | 内容 | 净估算 | 依赖 | 硬门禁 |
|---|---|---|---:|---|---|
| M0 | 已完成 | 基线冻结、ADR、golden | 1–2 人日 | 无 | 318 tests 绿、ADR Accepted |
| M1 | 未开始 | policy/weight/evidence/cache/临时授权 | 5–8 人日 | M0 | 自包含回归全绿 |
| M2 | 未开始 | 不可变 Core DTO 与 ports | 3–5 人日 | M1 合同 | Core 无 Paper/DB import |
| M3 | 未开始 | 版本化最小纵向切片 | 6–9 人日 | M2 | version→plan→score→persist 跑通 |
| M4 | 未开始 | 完整 AtomicRule 与生命周期生产接入 | 10–15 人日 | M3 | 越权分值不可生效、导入可追源 |
| M5 | 未开始 | Legacy adapters + ThesisProfile parity | 8–12 人日 | M4 | /papers 与论文 golden 兼容 |
| M6 | 未开始 | 通用持久化、v2 API、run-export@2 | 8–12 人日 | M5 | 非 Paper run 可持久化/复核/导出 |
| M7 | 未开始 | TechnicalProposalProfile 验收 | 5–8 人日 | M6 | 非论文 E2E 绿、无 Thesis 泄漏 |
| M8 | 未开始 | 默认切换、清理、文档与观察 | 3–5 人日 | M7 | Core 默认、旧入口仍兼容 |

净实现量约 49–76 人日；排期另加 25%–35% 的集成、数据、评审和返工储备，即约 61–103 人日。M1 的 evidence/cache 可有限并行，M6 的通用导出可与 M7 fixture 准备并行；M3–M5 的关键链不宜并行分叉。schema-driven Web 泛化列为后续 M9，不计入本轮后端估算。

### 3.2 M0：冻结基线与架构决策

任务：

- 保存一组代表性的论文 Mock 评分、人工复核、报告和 run-export@1 golden。
- 增加运行链 characterization tests，覆盖 deterministic、deductive、banded、hybrid。
- 记录当前 QWK/MAE/RMSE 基线和数据版本。
- 写 ADR，冻结以下决策：
  - Core/Profile 依赖方向。
  - weight 两种模式。
  - SourceQuoteEvidence 与 DeterministicObservation 分离。
  - ScopedAbsenceEvidence 的覆盖证明。
  - 模型不能授权分值。
  - RuleDecision 状态、max_points、occurrence 去重、band/deduct 顺序、bonus v1 阻断和 direction/effect 合法矩阵。
  - dependency/mutex 的 fail-closed 语义与 human override 权限边界。
  - version hash / policy hash / document hash 的运行身份。
  - hash_scheme 升级规则和 checker 版本固定方式。
  - business_profile_key 与 workflow_profile 的区别。
  - legacy 数据不伪回填。
  - 第二 Profile 采用 technical_proposal。
- 宣布 Core 改造期内：新增论文业务能力只能进入 ThesisProfile 或其过渡 facade，不再扩展 engine.py 的论文分支。
- 暂冻结 Streamlit 泛化；静态 Web 继续作为论文控制台。

完成定义：

- 全套 318 项测试绿色。
- golden 可在 Mock 下稳定复现。
- 关键术语统一为 Submission、DocumentSnapshot、RubricVersion、AtomicRule、Profile。
- ADR 中没有影响实施路径的未决语义。

完成证据（2026-07-18）：

- 实现提交：`bb5c3b9eb7f4656896ba7b30dfa19e13267e4955`（golden/测试/ADR/基线）与 `e07d632a96b9ec0f64f7900999d9fb40074615fd`（最终 occurrence/evidence identity 收口）。
- 迁移：无新增 schema 变更，最新仍为 `0011_version_hash_on_update`；全套迁移自检包含在 pytest 结果中。
- 测试：`.venv/bin/python -m pytest -q` → `318 passed, 5 warnings`；其中 M0 新增 6 项测试，覆盖四条 legacy 路由、Mock 重跑、人工复核、HTML 报告、`run-export@1`、ADR/术语和历史基线合同。
- Golden：`backend/app/tests/golden/m0/` 固定 Mock 评分、人工复核、完整报告和导出；动态 UUID/时间已归一化，业务结果保持全量对比。
- 决策：`docs/adr/0001-general-scoring-core-foundations.md` 状态为 Accepted，冻结 D01–D13；旧文档已声明以该 ADR 为准。
- 评估：`docs/baselines/m0-thesis-evaluation.json` 归档 n=98 的 QWK/MAE/RMSE 与运行元数据，仓库记录版本为 `m0-a4-full98-metadata-v1`。原 A4 dataset version/原始报告未被历史运行记录且原件未留存，因此明确保存为 unknown、`reproducible=false`、`gating_eligible=false`，不伪造数据指纹；后续可门禁基线必须按同目录 README 保存完整 identity。

### 3.3 M1：先修正确性基础

#### M1.1 ScoringPolicy 与 weight

- 先实现强类型 ScoringPolicy 和 Decimal 聚合/舍入函数。
- legacy_behavior_policy 只服务 golden/compare；M1 发布门禁通过后，所有新论文权威运行都使用 corrected_thesis_policy，包括仍由 legacy engine 产生的运行。历史运行不重写。
- 实施迁移 0012：ScoringRun 保存 policy_snapshot / policy_hash / policy_schema_version；ScoreItem 保存 aggregation schema、auto_score_status，并允许 invalid/blocked 时 ai_score 为空；ReviewLog 保存 policy_hash/resolution_type。新运行创建时即冻结，人工复核只读取该快照，禁止按部署时的当前 policy 重算。
- 迁移前的历史 run 没有冻结 policy 时，不允许跨版本自动重算；只能走显式 legacy-preservation 流程，或创建新的 rescore generation 并保留原运行。
- create、update、import、发布和评分开始前共用 weight 校验：全空或全有，混填/非正数失败。
- weighted_normalized 按 total_score × weight/Σweight × raw/max 计算，不要求权重使用特定总量尺度。
- v1 weight 必须能被现有 Numeric(6,2) 精确表示，即 0.01..9999.99 且最多两位小数；超范围或额外精度直接拒绝，不能静默量化。
- 初评分、人工改单项重算、submit_review 共用一个聚合函数。
- legacy engine 在 M1 后只负责产生 criterion raw score；总分贡献、等级、舍入和复核边界全部交给冻结 corrected_thesis_policy，避免等到 M8 才修复当前新运行。
- 非 100 分 rubric 按 policy 的 grade scale 正确评级。
- 单项结果记录 contribution。
- v1 等级使用舍入前总分，rounding.digits 限制为当前数据库可精确保存的 0–2 位；边界复核容差明确使用百分比点或原始分数单位。

最小验收例：

- 两项 max 均为 10，weight 80/20，得分 5/10 与 10/10，总分应为 60。
- 总分 20 的 rubric 得 20/20 时可按百分比 policy 进入最高等级。
- half_up 1 位下 2.25 得到 2.3。

#### M1.2 Evidence Validator

- quote strip 后为空即无效。
- Core 统一使用 evidence_unit_id；quote 不属于指定 evidence unit 时必须剔除。PaperChunk.id/chunk_id 仅作为 adapter 的非权威 source reference，不能进入验证或缓存身份。
- 只保留校验通过的 evidence。
- evidence_sufficient 由校验结果和 evidence policy 推导。
- mixed evidence 保留有效项，同时生成 ValidationIssue。
- injection flag 与 evidence validity 分开记录。
- deductive / banded 的决策证据也走同一验证器。
- 增加 ScopedAbsenceEvidence / CoverageEvidence，缺失事实必须记录 snapshot、检查范围和 evidence-unit IDs，不能用“检索不到”直接自动扣分。
- required evidence 无效时，该 criterion 的自动分和 run.final_total 均为空并进入复核；optional evidence 无效时只丢弃对应 effect，是否复核由冻结 policy 决定。
- M1 起 submit_review 不得通过普通改单项清除 required invalid/block；必须由授权 resolution 或新一代重评分解决。

#### M1.3 Cache / PromptEnvelope

- 把最小 canonical_json + PromptEnvelopeV1 提前作为第一个 Core 原语，M2 直接复用，不再另造一套 serializer。
- 让 LLM adapter 先构造最终 PromptEnvelope，再由 cache 和 provider 复用同一对象。
- M1 补齐标题、location、section_title、实际模型参数、policy 和 LegacyRubricAdapter 生成的 rubric_snapshot_hash；M3 再加入正式 version/plan identity。
- absence/coverage 判定还必须包含 declared scope、被检查 evidence-unit IDs 的 canonical hash、coverage completeness 和 evidence policy/version；覆盖范围变化必须 cache miss。
- key 对实际发送的原始 envelope 求 hash；账本分别保存 original hash、受控原文（若策略允许）和 redacted audit projection，不能把脱敏副本称为同一 envelope。
- 定义缓存保留期和访问边界。
- bump PROMPT_VERSION。

#### M1.4 deductive 过渡保护

完整 AtomicRule 执行器完成前：

- 实现最小 LegacyRubricAdapter，对旧 criteria/policy 生成 canonical snapshot 与 rubric_snapshot_hash；只要 Rubric 已有 provenance 但未合法发布，就 fail closed，不能伪装 legacy。
- 从显式 legacy deduction_rules_structured 派生 LEGACY:{criterion_code}:{rule_hash}，并把稳定 code 与冻结分值一同发给模型。
- 模型只返回命中的 legacy rule code；最终 points 从 LegacyAuthorizedRule 查表取得，不采用模型自报值。
- 未知、空 rule_ref、负 points 或超上限的 effect 不计分并按 policy 复核；required evidence 无效时该 criterion 与 run.final_total 必须为空。
- 没有可编译数值规则的 legacy deductive criterion 不形成自动最终分，转人工复核或显式走 legacy_direct_compat；不能因“不扣分”静默给满分。
- legacy_direct_compat 只能用于 source_kind=legacy_unversioned 的冻结旧标准：模型给分必须在 0..max_score、证据通过同一验证器且 policy 可强制复核；它不是 AtomicRule direction，也不能被新 RubricVersion 发布。
- 新 RubricVersion 路径不得接入旧的“任意 points 求和”函数。

完成定义：

- 新增 weight、非 100 分、half-up、空 quote、伪造 quote、混合 evidence、cache identity、越权 deduction 回归测试。
- 全套测试通过。
- PromptEnvelope 变化已 bump PROMPT_VERSION。
- M1 发布门禁由有权限的维护者在冻结 dataset/model/anchors 上重跑论文留出集，并记录所有 identity 与基线变化；没有真实环境时可在 feature flag 后合并，但不能据此切默认 Core。

### 3.4 M2：定义不可变 Core 合同

任务：

- 建立 scoring/core 目录并实现架构方案 §2 的 DTO。
- 从现有 ScoringInputs / ScoringResult 提炼，不平行重写现有算法。
- 建立 LLMRuntime、CacheLedger、EvidenceRetriever、CheckerRegistry、Clock 等显式 ports。
- 编写 canonical JSON 与 hash 工具，包含 schema version。
- 明确 source_artifact_hash、normalized_content_hash、document_snapshot_hash，并生成不依赖数据库 UUID 的稳定 evidence_unit_id。
- metadata 默认不进 prompt；只有 Profile 明确选择的判分字段进入 PromptEnvelope。
- 增加 import boundary test，阻止 core 引用 Paper、SQLAlchemy、论文 coherence 或报告模块。
- 增加最小 technical_proposal contract fixture / TestProfile，提前消费 snapshot、prompt selection、registry 和 policy 隔离合同；M7 再完成真实业务 Profile。

完成定义：

- DTO round-trip 测试。
- 字典顺序、对象 ID、路径变化不影响应稳定的 hash。
- 内容、policy、profile 或 rule 变化必然改变相应 hash。
- Core 单元测试无需 DB、文件系统和网络。

### 3.5 M3：版本化最小纵向切片

目标不是一次实现所有规则，而是尽早证明新模型能进入真实运行链。

任务：

- 增加迁移 0013 的版本、plan、document、engine 与幂等审计字段及约束；policy 字段沿用 0012。冻结 content-addressed DocumentSnapshot ref/hash，只保存孤立 hash 不算可重放。
- 新 Core run 保存 rescore_generation 与由完整输入身份生成的 idempotency_key；数据库唯一约束决定唯一权威 run，冲突时校验全部输入 hash 后返回已有 run，不能用“先查再插”承担并发正确性。
- 在增加 RubricVersion 新列前，把 rubric-content-v1 改为当前 0011 图字段的显式白名单；hash_scheme 自身和 business_profile_key 不进入 v1。既有版本回填 thesis/v1，新版本使用包含 profile 的 rubric-content-v2。
- 实现 CompiledRubricSnapshotLoader：
  - 只接受已发布版本。
  - 按声明的 rubric hash scheme 重算完整图，并校验 RubricVersion.version_hash 与发布 compilation hash。
  - 一次性加载完整图并脱离 ORM。
- 先建立带不可变版本 key 的 CheckerRegistry 骨架，再构建 plan。
- 实现 RuleExecutionPlanBuilder 的最小版本：
  - 稳定排序。
  - policy 编译。
  - checker key 解析。
  - plan hash。
- 实现 score_submission() 的最小纵切：
  - 一个 deterministic deduct rule。
  - 一个由 TestProfile / technical_proposal contract fixture 驱动的 semantic band rule。
  - policy 聚合与证据验证。
- 完成 LegacyRubricAdapter 的 Core snapshot 适配；实现 LegacyPaperAdapter 雏形，把现有 Paper/ParsedPaper 转为稳定 DocumentSnapshot。
- 以 SCORING_ENGINE_MODE=compare 接入 score_paper()，默认仍 legacy。
- compare 只生成非权威 comparison artifact；另用 SCORING_ENGINE_MODE=core 的隔离集成测试验证新运行可持久化完整 rubric identity/profile/policy/document/engine identity。

完成定义：

- 测试 fixture 的已发布 RubricVersion 可以从数据库加载为 plan。
- plan 可在 core 模式集成测试中对一篇论文 fixture 完成评分并写入现有 ScoringRun/ScoreItem。
- compare 模式的正式 ScoringRun 仍只有 legacy 权威结果，Core 候选不进入报告或复核。
- rubric snapshot、policy compiler、business profile、checker manifest 和 engine contract 全部相同时，plan hash 稳定。
- 同一已发布 v1 fixture 在 0011→0012→0013 后重算 hash 完全不变；新 v2 版本的 profile 变化会改变 hash。
- 并发相同请求只产生一个权威 run；显式 rescore generation 产生新 run，idempotency collision fail closed。
- 未发布版本、hash 不一致、profile 不匹配、未知 checker 均无法开始评分。
- /papers 和 score_paper() 的外部合同不变。

### 3.6 M4：完整 AtomicRule 执行与生命周期生产接入

#### M4.1 执行器

- 实现架构方案冻结的 RuleDecision 状态、direction/effect 合法矩阵、criterion 起始分与应用顺序。
- v1 支持 deduct、band、none；bonus 保留为模型枚举中的未启用值，发布验证必须阻止，直到后续合同显式定义 base_score 与 ceiling。
- 支持 score、review、block_submission、report_only。
- 支持 once、per_occurrence、capped。
- 支持 depends_on_rule_codes。
- 支持 mutex 冲突 fail closed。
- 用稳定 occurrence_id 去重并保证重复请求幂等。
- band 只能选择现有 RuleLevel。
- RuleLevel.points 与所有自动 criterion score 必须落在 0..criterion.max_score；once、per_occurrence、capped deduct 均要求固定 max_points。
- 每个 score effect 记录 version_hash、rule_code、decision、evidence、calculated effect。
- Checker 只返回 observation；分值由 RuleExecutor 决定。

#### M4.2 发布前可执行性验证

发布 blocker 至少包括：

- compilation 未 validated 或 blockers 非空。
- AtomicRule 未 approved，或审核人/时间/human_changes 不完整。
- deterministic rule 缺 checker_key。
- checker 未注册或参数 schema 不通过。
- semantic rule 缺必要 evidence policy。
- band 没有合法 RuleLevel。
- depends_on 成环或引用不存在。
- mutex 定义不一致。
- 已存在且被 workflow policy 要求确认的 RuleTemplateLink 仍为 pending。
- global_policy schema 不受支持。
- business_profile_key / workflow_profile 未注册或 submission/profile 不匹配。
- criteria / weights / total_score 不满足 policy。
- scoring_mode=llm_direct，或 hybrid 子项仍依赖 llm_direct，且未被显式迁为 band/deduct/review-only；正式版本不得发布兼容执行节点。

#### M4.3 导入与编译双写

- 扩展导入 DTO，保留：
  - 文件 hash / size。
  - sheet、row、cell locator。
  - Word section / comment / source locator。
  - parser/compiler/prompt/model 版本。
- 文件解析和 LLM 编译在数据库事务外完成；验证后的完整内存图在一个短事务中原子生成：
  - 旧 Rubric / RubricCriterion 投影。
  - RubricCompilation / SourceArtifact / SourceRule。
  - TemplateItem / RuleTemplateLink。
  - RubricVersion / AtomicRule / RuleLevel。
- LLM 生成的规则只能是 draft，必须人工确认后发布。
- 导入器不再把缺少档位/结构化扣分规则的项默认变成可发布 llm_direct；应生成带 blocker 的 draft，并要求选择 band、deduct 或 review-only。
- AtomicRule 审批审计复用 RubricCompilation.human_changes，但冻结逐条事件 schema：change_id、rule_code、field_path/action、before/after、actor_id、occurred_at、reason。approved/rejected 必须存在与 reviewed_by/reviewed_at 一致的 approve/reject 事件，发布时校验并随 compilation 冻结。
- provenance/AtomicRule 图是正式真相，旧 RubricCriterion 是派生兼容投影；draft 修改经同一服务重建投影，禁止分别编辑形成双真相。
- 正式 hybrid 在发布编译时展开为稳定编码的独立叶子 criteria；每个叶子只采用一种执行语义。points 模式直接求和；weighted 模式要求显式、可精确表示的叶子 weight（或由编译器按 max_score 比例精确派生，否则阻断发布），避免对父项重复加权。父子关系仅作来源与展示分组。

#### M4.4 API / CLI 生命周期接入

- create/import 进入新 compilation 流程；手工 JSON 创建生成 manual_json SourceArtifact/SourceRule。
- 提供 AtomicRule 编辑、提交 review、approve/reject，以及必需 RuleTemplateLink confirm/reject 的服务/API/CLI，完整记录 reviewer/time/human_changes。
- 增加 draft → review、review → draft API。
- publish 调用 services/rubrics/lifecycle.py，不再直接改 status。
- clone 调用深克隆服务，不再只复制 criteria。
- CLI publish 使用同一服务。
- batch 创建时，新标准必须锁定已发布 rubric_version_id。
- legacy draft 必须先显式 upgrade 成完整 provenance graph；取消旧直接发布路径。已发布但不符合新验证的图只能 clone 后修复。

完成定义：

- 每个扣分可追到 AtomicRule 和 SourceRule / TemplateItem。
- 模型伪造 rule_ref 或 points 不能改变分数。
- repeat/cap/dependency/mutex/effect_type 均有独立测试。
- 真实 Excel+Word fixture 完成导入、复核、发布、计划加载和评分。
- manual_json rubric 与 legacy draft upgrade 也走同一签核链。
- API 与 CLI 不再绕过生命周期服务。

### 3.7 M5：LegacyPaperAdapter 与 ThesisProfile parity 接入

任务：

- 完成 Paper + ParsedPaper + PaperChunk → SubmissionSnapshot / DocumentSnapshot。
- ThesisProfile 显式注册：
  - 论文结构。
  - 字数参数化检查。
  - 图表引用。
  - 引文—参考文献。
  - 格式。
  - 研究问题—结论一致性。
- 删除新路径中的 criterion.name 中文分派；旧实现只保留兼容 facade。
- 把固定 3000 字和扣分比例改为 checker_params / AtomicRule。
- 把论文提示词从 LLM provider 移入 ThesisProfile。
- 把论文 Mock 逻辑、报告字段、JSON/Excel 扩展和 QWK dataset builder 归入 Profile。
- 复核只写 human override，不修改 AI/RuleDecision；block/invalid 不能被普通改单项直接清除，未解决时 final_total 为空。
- 显式 core 路径下，score_paper() 仅负责：
  1. 读取 legacy Paper。
  2. 构造快照和 plan。
  3. 调用 score_submission()。
  4. 通过兼容持久化 adapter 写回现有表。
- 在 compare 和显式 core 模式完成论文兼容验证；生产默认值仍保持 legacy，统一留到 M8 切换。
- LegacyRubricAdapter 把冻结旧 llm_direct 映射为 LegacyDirectCriterionNode，把旧 hybrid 映射为 CompositeCriterionNode；子项各自计分、父项求和并校验 0..max_score，越界即 invalid/block，parent weight 只应用一次。两种兼容节点仅允许 legacy_unversioned。

完成定义：

- score_paper() 外部签名不变；显式 core 模式下通过 adapter 调用 score_submission()。
- legacy 默认路径在 M8 前保留。
- 现有 /papers、CLI、报告、人工复核和历史查询继续工作。
- Core 包不包含论文默认检查或 prompt。
- 所有旧测试和新增兼容 golden 通过。
- M5 发布门禁由有权限的维护者固定 dataset/model/anchors 重跑论文 QWK，并说明因正确性修复造成的合理差异；缺少真实环境时保持 feature flag，不切默认。

### 3.8 M6：通用持久化、v2 API 与导出

任务：

- 实施迁移 0014。
- 把 DOCX/PDF 的段落、样式、页码、表格提取与 Profile 结构解释分开；v2 ingestion 不再先构造 ParsedPaper。
- 新增 /api/v2/submissions：
  - 创建/上传。
  - 解析状态。
  - 获取 DocumentSnapshot 摘要。
  - 发起评分。
  - 获取 run。
  - 人工复核。
- 保留 /papers；ThesisProfile 可继续由旧 API 使用。
- 新增 run-export@2：
  - schema/version。
  - submission。
  - rubric version / plan / policy identity。
  - criterion / rule outcomes。
  - evidence / observations。
  - review logs。
  - profile_extensions。
- 保留 paper-grading/run-export@1。
- 提供通用 HTML 和 Excel 基础布局，Profile 只扩展元数据与专题 findings。
- CLI 增加显式 profile 选择，但默认 thesis 保持兼容。
- eval 注入 GradeScale / ScoringPolicy，不再固定 60/70/80/90。
- paper_id / submission_id 使用数据库 exactly-one CHECK；产生 submission-only 数据后 downgrade 明确拒绝，不能伪造 Paper 或删除 run。

完成定义：

- 最小 technical_proposal contract fixture 可作为非 Paper submission 持久化、评分、复核、查询和导出。
- v1 与 v2 输出各自有 schema golden。
- 旧历史 run 仍可生成旧报告。
- 新 run 的审计身份完整。

### 3.9 M7：第二个非论文 Profile 验收

采用“软件项目技术方案评审”作为验收场景。它与论文差异足够大，同时能复用 DOCX/文本解析基础。

元数据：

- proposal_id。
- vendor_name。
- project_name。

结构：

- 需求理解。
- 总体方案。
- 实施计划。
- 风险控制。
- 服务承诺。

示例规则：

- core.required_sections：按 checker_params 检查必需章节。
- core.text_length_range：按模板参数检查，不存在 3000 字默认值。
- semantic band：需求理解与总体方案匹配度。
- semantic deduct：风险项缺概率、影响、措施或负责人时，只能按已发布 AtomicRule 扣分。
- compiled hybrid：里程碑完整性与实施可行性在发布时展开为两个独立叶子 criteria，前者由代码规则判断，后者由 semantic band 判断；不得发布 llm_direct 子项。

至少准备两个固定 Mock E2E fixture：一个完整方案，一个故意缺失/低质量方案；二者都不含论文摘要、目录、参考文献和结论，用来验证 ThesisProfile 不会泄漏。

完成定义：

- technical_proposal RubricVersion 可导入、复核、发布。
- v2 submission → snapshot → plan → score_submission → review → run-export@2 全链绿色。
- 所有扣分 rule_ref 属于已发布 AtomicRule。
- 结果记录 profile/version/policy/plan/document/engine hash。
- thesis 与 technical_proposal 可在同一进程连续运行，registry、prompt、cache、grade policy 互不污染。
- 固定 Mock 下精确断言 RuleDecision、contribution、总分、等级和 review 状态，而不只断言链路成功。
- 使用非 100 总分、自定义等级、不同舍入与复核容差，证明 global_policy 生效。
- 修改规则名称但保留 checker_key 时，checker 路由和确定性分值语义不变；因名称进入语义 prompt，version/cache identity 必须正确失效。
- 同一内容仅改变 profile 必须 cache miss；proposal_id/vendor_name/project_name 经 v2 API 与 run-export@2 完整 round-trip。
- 结果中不存在 Thesis findings 或学生字段。

### 3.10 M8：默认切换、清理和发布观察

任务：

- 将 SCORING_ENGINE_MODE 默认设为 core。
- 切换前盘点所有活跃 rubric：published_version 必须通过 plan 构建；冻结 legacy_unversioned 必须可由兼容节点执行；存在未发布 provenance 图、未知 checker 或不可迁移规则的批次必须阻断，不能静默回退 legacy。
- 旧内部函数标记 deprecated，但暂不删除外部兼容入口。
- 清除新路径中的中文名称路由、固定 3000 字、固定百分制等级和固定扣分比例。
- 更新 README、设计文档、迁移说明、Web/CLI 功能矩阵和 eval 文档。
- 修正静态 Web 的旧 80%/论文口径，但 profile metadata schema 驱动的全面泛化后置 M9；Streamlit 不作为 v2 首发门禁。
- 增加观测指标：
  - legacy/core score delta。
  - invalid evidence rate。
  - unauthorized rule rate。
  - manual review rate。
  - cache hit/miss by profile/version。
  - checker failures。
- 切换前为上述指标定义阈值、观察窗口和回滚到 legacy 的条件。
- M8 发布门禁包含固定 dataset/model/anchors 的最终论文评估记录；无真实评估环境不得切生产默认值。
- 至少经过一个稳定发布观察期，再决定是否移除 legacy compute 内部路径。

完成定义：

- 新评分默认走 Core。
- 兼容 API 与历史数据可用。
- 两个 Profile 的端到端测试在 CI 中长期运行。
- 文档中的迁移号、命令、产品定位和实际实现一致。

---

## 4. 建议 PR / 提交切片

每个里程碑必须以可运行纵向切片结束。DTO/迁移等基础 PR 可以不是完整纵切，但必须有直接消费者测试，且不能长期悬空。

1. PR-00：刷新计划、ADR、golden 和 characterization tests。
2. PR-01：canonical_json、PromptEnvelopeV1、LegacyRubric snapshot/hash。
3. PR-02：Evidence Validator 与 absence/coverage evidence。
4. PR-03：迁移 0012、ScoringPolicy 冻结、Decimal 舍入、weight 与统一重算。
5. PR-04：LegacyAuthorizedRule 与 deductive fail-closed。
6. PR-05：cache 使用真实 PromptEnvelope，bump PROMPT_VERSION。
7. PR-06：Core DTO、ports、TestProfile 和 import boundary tests。
8. PR-07：迁移 0013、snapshot refs、幂等与运行身份约束。
9. PR-08：版本化 CheckerRegistry 骨架。
10. PR-09：Rubric snapshot loader、policy compiler、plan builder/hash。
11. PR-10：最小 score_submission() 纵向切片。
12. PR-11：LegacyPaperAdapter + compare artifact。
13. PR-12：AtomicRule deduct/band、occurrence 与 evidence 授权（bonus v1 阻断）。
14. PR-13：dependency/mutex、非 score effects 与 human override。
15. PR-14：导入 provenance 真相图 + 兼容投影。
16. PR-15：规则/link 审批、发布验证、API/CLI 生命周期。
17. PR-16：ThesisProfile checker / prompt / Mock facade。
18. PR-17：ThesisProfile report/export/eval facade。
19. PR-18：论文显式 core 路径、兼容 golden 与 QWK 重锚。
20. PR-19：迁移 0014 与通用持久化。
21. PR-20：/api/v2/submissions 与通用复核。
22. PR-21：run-export@2 与通用报告/Excel 基础。
23. PR-22：CLI profile 入口与 eval GradeScale 注入。
24. PR-23：TechnicalProposalProfile 双 fixture E2E。
25. PR-24：生产默认切换、文档收口与 legacy 内部路径弃用。

PR-02、PR-03、PR-04 可在 PR-01 后有限并行；PR-07 至 PR-18 的运行关键链按依赖顺序推进。PR-19 完成后，PR-20 与 PR-21 可并行。

---

## 5. 测试与质量门禁

### 5.1 每个评分逻辑 PR

- 先添加失败回归测试。
- 运行相关专项测试。
- 运行全套：

    .venv/bin/python -m pytest -q

- 修改 prompt / 输入构造必须 bump cache/llm_cache.PROMPT_VERSION。
- 每个 PR 的硬门禁只依赖自包含 Mock/golden/属性测试和全套 pytest。
- M1、M5、M8 发布门禁再由有权限的维护者在冻结 dataset/model/anchors 上运行真实留出集，记录 QWK、MAE、RMSE、复核率及所有 identity。
- 无真实模型/数据环境时允许在 feature flag 后合并，但不得切换生产默认 Core。
- 不用“旧分数完全相同”否决明确的正确性修复，但必须解释差异。

### 5.2 Core 合同测试

- DTO round-trip。
- canonical hash 稳定性和敏感字段测试。
- policy schema。
- weight/rounding property tests。
- evidence 正反例。
- absence/coverage evidence 的完整范围与失败用例。
- 未授权 rule / level / points。
- repeat/cap/dependency/mutex。
- checker params schema。
- checker implementation/version 和 profile mismatch。
- import boundary。

### 5.3 集成测试

- 已发布 version → plan → score_submission。
- LegacyPaperAdapter → ScoringRun/ScoreItem。
- API / CLI 生命周期。
- 人工改单项后按冻结 policy 重算。
- human override 不修改 RuleDecision，block/invalid 未解决时 final_total 为空。
- cache hit/miss 精确身份。
- 重复请求、并发 persist 和 occurrence 去重的幂等测试。
- run-export@1 兼容和 run-export@2。

### 5.4 迁移测试

- 0011 → 0012、0012 → 0013、0013 → 0014 逐段 upgrade/downgrade。
- 0012 出现任何不可由旧表表达的冻结 policy/aggregation/invalid score 后拒绝 downgrade。
- 0013 出现新 profile/hash scheme 的版本、batch version pin、run snapshot/identity/idempotency 等任一新数据后拒绝 downgrade；测试“已发布但未评分”的新版本。
- 0014 只要存在 evaluation batch、submission 或 document snapshot（包括已上传但未评分）即拒绝 downgrade，不以 submission-only run 为唯一条件。
- 历史 null version 路径。
- SQLite 外键与事务。
- 默认全套测试继续使用自包含 SQLite；另设可选 Postgres CI job 验证约束、迁移和稳定排序。
- Alembic 自检独立于 pytest create_all 路径。

### 5.5 Profile 隔离测试

- Core 不生成 Thesis 术语。
- 同进程先后评分 thesis 与 technical_proposal。
- registry 无跨 Profile key 泄漏。
- cache key 包含 profile。
- prompt、grade policy、metadata schema 和 export extensions 隔离。

---

## 6. 主要风险与控制

| 风险 | 控制 |
|---|---|
| 正确性修复导致历史分数漂移 | 历史 run 不回写；新 run 固化 engine/policy/version；重评分生成新记录 |
| 新旧 Rubric 成为双真相 | 正式运行优先不可变 RubricVersion；legacy 明确标识并计算内容 hash |
| 模型越权扣分 | 模型只返回 verdict/rule code；代码按 AtomicRule 算分；未知规则零影响并复核 |
| evidence 严格化导致复核率上升 | 视为预期安全结果；更新 Mock/fixture；用留出集重新定基线 |
| cache 污染 | hash 直接来自真实 PromptEnvelope；profile/policy/checker/version 全入 key |
| Registry 变成任意代码执行 | 仅显式注册、参数 schema、版本 manifest；禁止动态 import |
| Profile 接口过度设计 | 先做仓库内 Protocol，只抽两个实际 Profile 共同需要的能力 |
| 三端同时改造失控 | Core/API/CLI 优先；静态 Web 后置；Streamlit 冻结 |
| 第二案例太像论文 | 使用不同 metadata、结构、grade policy，并断言无 Thesis findings |
| 生命周期模型继续旁路 | 每个 PR 都必须包含运行纵切，不接受只加表/DTO 没有调用方 |
| 发布验证遗漏 | plan builder 与 publish validator 共用同一可执行性校验 |
| QWK 被误用 | 每个 Profile 独立基线；E2E 证明架构，不证明准确度 |

---

## 7. 立即启动顺序

1. 完成 PR-00：ADR、golden、characterization tests，并关闭 M0。
2. PR-01 先固定 canonical/PromptEnvelope/legacy rubric identity。
3. 以 PR-02、PR-03、PR-04 修 evidence、policy/weight 和 legacy deductive 授权。
4. PR-05 完成真实 PromptEnvelope cache key 后，运行全套自包含门禁并由维护者执行 M1 真实评估门禁。
5. M1 通过后进入 PR-06～PR-11 的最小 version → plan → score_submission → adapter 纵切。

## 8. 项目级完成定义

项目完成本轮通用化，不以“新增若干类或表”为标准，而以以下运行事实为标准：

- 原 312 项测试持续通过，并新增 Core/Profile/兼容/迁移测试。
- score_submission() 不依赖数据库、Paper 或文件路径。
- score_paper()、/papers、旧表和历史报告继续可用。
- 所有新 Core 运行记录 rubric_source_kind、rubric_snapshot_hash、profile、policy、plan、document 和 engine identity。
- published_version 运行另记录 rubric version ID/hash；legacy_unversioned 运行保持 version ID/hash 为空。
- weight、rounding、grade、review boundary 均由 ScoringPolicy 控制。
- 语义 evidence 不允许空 quote；确定性 observation 使用独立审计类型。
- deductive / banded 只能引用已授权 AtomicRule / RuleLevel。
- checker 调度只依赖 checker_key，不依赖中文名称。
- 发布时即验证 policy、规则状态、依赖、档位和 checker 可执行性。
- ThesisProfile 与 TechnicalProposalProfile 均有端到端测试，且同进程互不泄漏。
- 新旧 Rubric 生命周期只有一条正式发布路径。
- README、代码改造计划、迁移号、Web/CLI 矩阵和 eval 文档与实际实现一致。

---

## 9. 状态维护规则

本文件从本版起只记录当前主线，不再混写已完成历史项目：

- 每个里程碑只使用 未开始 / 进行中 / 已完成 / 阻塞 四种状态。
- 完成必须附 PR/commit、迁移号、测试数量和验收证据。
- 测试基线、迁移号或默认运行链变化时，同一 PR 更新本文件顶部。
- 已完成的详细过程移入执行进展或 release notes，不继续膨胀当前计划。
- 任何新论文需求先判断属于 Core 还是 ThesisProfile；无法说明边界的需求不得直接进入 engine.py。

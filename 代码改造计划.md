# 论文打分系统 · 改造计划（产品导向版）

> **本版按产品理念重规划并覆盖旧版。** 产品 = **模板驱动的打分系统**：用户先提交**模板**→系统据模板对后续**待评文件**打分并**记录**→打分有问题由**人工复核**干预。
> 两种形态：**Web**（联网服务，用户经前端通过网络使用）/ **CLI**（全本地离线，可把评分代码拉到本地运行，不依赖网络）。
> **LLM 双轨**：云厂商 **APIKey** 调用 或 **本地私有模型** 调用——由用户决策。
> 刷新日期 **2026-06-18**；测试基线 **168 绿**；迁移 **0007**；分支 `feat/cli`。
> 旧版"对齐《设计方案》评分方法论"主线已基本完成（详见本文件 git 旧版 + `执行进展.md`）；本版聚焦**产品形态完整性**。

---

## 0. 产品定位（对齐确认）

| 维度 | 定位 |
|---|---|
| 核心流程 | **模板 → 打分 → 记录 → 人工复核**（评分项来自用户授权的模板，不写死） |
| 形态 A · Web | 联网服务，前端 + FastAPI，多用户经网络使用 |
| 形态 B · CLI | 全本地离线，`git clone` + 安装即用，配本地模型后**零网络依赖** |
| LLM 双轨 | 云 APIKey（OpenAI / 智谱 等）**或** 本地私有模型（llama.cpp / Ollama / vLLM），用户决策 |
| 评分能力定位 | **粗档辅助评分 + 人工复核**。QWK 实测≈0（结构性天花板，n=98），但绝对校准好（90% 落一档内）。**这与产品"打分有问题→人工复核干预"的理念天然契合**——系统给大方向，人定终分 |

---

## 1. 现状 vs 产品理念 · 差距速览

| 产品支柱 | 现状完成度 | 差距（本计划要补的） |
|---|---|---|
| 模板→打分→记录→复核 | **~90%** ✅ | 主流程齐全（rubric 导入/三模式评分/ScoringRun 记录/ReviewLog 复核）；缺：模板预检反馈、批量管理打磨、Web/CLI 功能对等 |
| Web 形态 | **~80%** 🟡 | Streamlit + 静态 Web + FastAPI 齐；**缺真实鉴权/多用户隔离**（现 `dev-user` 占位）——联网服务的前置 |
| CLI 全本地离线 | **~70%** 🟡 | `pgs` 15 子命令 + `--no-db` 已可本地跑；**缺"纯本地"硬保证**（网络触点审计 + 离线开关）+ 本地模型 onboarding + 一键安装 |
| LLM 云轨（APIKey） | **~95%** ✅ | openai / openai_compatible(智谱) 完备；仅默认值/预设待理顺 |
| LLM 本地轨（私有模型） | **~60%** 🟡 | **机制已通**（openai_compatible + 本地 base_url，已实跑 llama.cpp）；**缺产品化**：概念清晰化、预设、自检、文档、默认离线友好 |

> 一句话：**评分内核与双形态骨架已成型，本计划把"LLM 双轨 + CLI 离线 + Web 鉴权"从"能用"做到"产品级"。**

---

## 2. 目标架构（产品形态）

```
                    ┌─────────────────── 共享评分内核 (backend/app) ───────────────────┐
                    │  rubric_import(模板解析/编译)  scoring/engine(三模式+确定性+证据)   │
                    │  checkers / coherence / calibration / cache(L0) / eval(QWK)        │
                    │  llm/ 抽象层 ──► [ 云: openai/openai_compatible ] [ 本地: 同协议+本地base_url ] │
                    └───────────────┬───────────────────────────────┬───────────────────┘
        Web 形态（联网）            │                               │       CLI 形态（本地离线）
  前端(static/streamlit) → FastAPI ─┤                               ├─ pgs(Typer) → 本地 sqlite / --no-db
  鉴权 + 多用户 + 网络导出(Sheet)   │                               │  本地模型 + 离线保证 + 一键安装
                    └──────────────► Postgres                       └──────────► 无外呼
```

**LLM 双轨抽象（关键）**：云与本地**走同一 OpenAI 兼容协议**，差异仅在 `base_url` / `api_key` / `model`。产品把"选云还是选本地"提升为**一等配置决策**，而非让用户去猜 `LLM_PROVIDER=openai_compatible` 该怎么填。

---

## 3. 改造路线（按产品支柱分阶段）

> **状态（2026-06-19）：P1–P5 全部已实施 ✅**　测试 168→**182 绿**，迁移到 **0008**，本会话 7 个产品化 commit（`03d8f82`…`d0679ec`）。
> 各阶段落地：P1 本地模型一等支持 + 触网指示 + CLI 旗标 + Web 徽标；P2 `pgs doctor` + OFFLINE_MODE 守卫 + 本地模型/离线文档；P3 结构化 JSON 导出 + 功能对等矩阵；P4 单租户简单登录(opt-in) + owner_id 预留(0008) + QWK 定位说明；P5 `pgs init` 引导 + 部署文档。
>
> 标注：**[已具备·仅产品化/文档]** vs **[需新建]**，诚实区分工作量。

### P1 · LLM 双轨产品化（核心，最高优先）

目标：用户能清晰地在"云 APIKey"与"本地私有模型"间选择，两者皆一等公民、皆有自检与预设。

- **P1.1 概念清晰化** [需新建·小]：在 factory 增 `local` provider 别名（映射到 openai_compatible + 本地默认 base_url，如 `http://localhost:11434/v1`），让用户填 `LLM_PROVIDER=local` 即可，不必理解兼容层内幕。
- **P1.2 预设配置** [需新建·小]：`.env.example` 给四套即用块（取消注释即用，**本地·llama.cpp 为主推**）——① 本地·llama.cpp（主推，`llama-server`） ② 本地·Ollama（备选） ③ 云·OpenAI ④ 云·智谱；每套含 base_url/model/key 示例与说明。
- **P1.3 双轨自检** [已具备·补强]：`/system/llm-check`、`pgs check` 已能测连通；补"本地不可达"的清晰报错（连不上 `localhost:port` → 提示"本地模型服务未启动/端口错误"），区分"未配置/连不上/响应非法"。
- **P1.4 默认离线友好** [已具备·补自检/文档]：`LLM_PROVIDER` **已默认 mock**（零配置跑通、mock 不触网）；本项收窄为 `pgs doctor`/llm-check **明示当前 provider 是否触网**，文档强调"选 openai_compatible/openai 即外呼、选 local 即连本地端口"。
- **P1.5 Web/CLI 配置入口** [需新建·中]：CLI 增 `--provider/--model/--base-url` 旗标（覆盖 env）；Web 仅**只读 provider 状态卡 + 连通自检**（§4 已定：不做密钥编辑 UI，配置走 env）。

### P2 · CLI 全本地离线闭环（核心）

目标：选本地模型时，CLI 端到端**零网络依赖**，并能自证"我现在是纯本地的"。

- **P2.1 网络触点审计** [需新建·小]：盘点所有外呼——LLM(本地=离线)、Google Sheets 导出(网络)、模型/依赖下载、任何默认云 base_url、潜在 telemetry/更新检查。产出"离线就绪清单"。
- **P2.2 离线保证开关** [需新建·中]：`pgs --offline`（或 provider=local 时自动）下，禁止一切外呼：网络型导出(Sheet)优雅禁用并提示用 Excel；`pgs doctor` 报告"当前配置是否纯本地 + 哪个环节会触网"。
- **P2.3 本地模型接入指南** [需新建·文档]：以 **llama.cpp 为主线**——`llama-server` 自带 OpenAI 兼容端点（已实战，见 `docs/QWK基线攻坚-对话全记录.md`），含模型选型/量化/显存/端口建议；**Ollama 作备选简述**（`ollama` 起 `/v1`）。
- **P2.4 一键安装分发** [需新建·中]：`pip install .` / `uv` 安装 `pgs`（`pyproject.toml` 已配 `[project.scripts]`）；补安装脚本 + 离线依赖说明（`uv.lock` 已在）；可选 PyInstaller 单文件分发。

### P3 · 主流程产品打磨 + Web/CLI 一致

目标：模板→打分→记录→复核在两端体验一致、完整、可运维。

- **P3.1 模板生命周期** [已具备·打磨]：上传/解析/校验/版本/发布在 Web 与 CLI 均顺畅；增**模板预检反馈**（哪些规则解析成功、哪些需人工确认 draft→publish）。
- **P3.2 批量打分管理** [已具备·打磨]：批量上传/进度/失败重试/断点续（L0 缓存已支持续跑）；大批量的稳定性与可观测。
- **P3.3 复核闭环完整性** [已具备·补强]：Web 复核页 + `pgs review` 已有；补"复核建议 / AI-人工差异高亮 / 批量复核"。
- **P3.4 记录与导出** [已具备·补]：HTML 报告齐；补**结构化 JSON 导出**（§11，便于二次处理）；离线时 Excel 本地导出，Sheet 仅联网可用。
- **P3.5 Web/CLI 功能对等矩阵** [需新建·小]：列两端功能差异表，补齐关键缺口，明确各自定位（Web=协作/多用户；CLI=本地/批处理/隐私）。

### P4 · 技术债收尾（质量与正确性）

- **P4.1 CodeRabbit 待决项** [需决策后修]：retrieval/keyword.py 5 处（**`ORDER BY` 缺失在 Postgres 下非确定性**最值得修）、papers 摄取事务、eval 列名匹配、rubric 权重 all-or-none。改动多涉及证据召回→须 bump `PROMPT_VERSION` 重锚（QWK 已定论≈0，主要为**可复现性/Postgres 正确性**而非追 QWK）。完整清单见 git 旧版 §3.2 + 记忆 `coderabbit-deferred-design-items`。
- **P4.2 QWK 定位落地** [文档/UX]：报告与产品文案明确"**粗档辅助 + 人工复核**"定位，避免用户误期为排序器。
- **P4.3 鉴权与审计（单租户起步）** [需新建·中]：§4 已定**单租户 + 简单登录**——user 表 + session/token + 登录页/中间件；created_by/reviewer 改取登录用户（不再硬编码 `dev-user`）。**数据模型预留 `owner_id`**（Rubric/GradingBatch/Paper/ScoringRun，迁移 0008，单租户暂不强隔离）为将来多用户铺路。CLI 本地单用户可豁免登录。是 Web 公网上线的硬前置。

### P5 · 部署与上手

- **P5.1 Web 部署** [已具备·补文档]：`docker-compose.yml` 已有；补部署文档（Postgres/环境变量/反代/HTTPS）。
- **P5.2 CLI 分发**：见 P2.4。
- **P5.3 首次运行向导** [需新建·小]：`pgs init` 引导选 provider（云/本地）、配模型、建首个模板，降低上手门槛。

---

## 4. 已定决策（2026-06-18 用户拍板）

1. **Web 单租户起步** ✅：先**单租户 + 简单登录**（user 表 + session/token，不做完整 RBAC）；**数据模型预留 `owner_id` 字段**（Rubric/GradingBatch/Paper/ScoringRun 等，单租户暂不强隔离，为将来多用户铺路）。→ 收敛 P4.3 工作量。
2. **本地模型主推 llama.cpp** ✅：onboarding/预设/文档以 **llama.cpp（`llama-server` 自带 OpenAI 兼容端点）** 为主线（已实战验证，见 `docs/QWK基线攻坚-对话全记录.md`）；Ollama 作备选简述。底层仍走 openai_compatible 抽象，不锁死。
3. **Web 配置只读 + env** ✅：provider/base_url/model/key 经 **env / `.env`** 配置；Web 仅提供**只读 provider 状态卡 + 连通自检**，**不做密钥编辑 UI**（规避密钥加密存储/权限的安全工作量）。
4. **默认 provider = mock** ✅：现配置**已默认 `mock`**（`config.py`），保持——零配置可跑通且离线友好，**provider=mock 时绝不触网**；仅当用户显式选 `openai_compatible/openai/local` 才外呼或连本地。P1.4 因此收窄为"自检并文档化：哪个 provider 会触网"。
5. **CodeRabbit 待决项**：择机随重锚处理，**retrieval `ORDER BY`（Postgres 确定性）优先**，其余随产品需要。

---

## 5. 实施进度（历史，指针）

| 阶段 | 内容 | 测试 |
|---|---|---|
| 1–20 轮（设计方法论对齐） | 三模式/确定性/证据/L0/L2/coherence/eval/CLI/纯 Core 解耦 | →133 |
| C 段（能力增量） | track-changes / 漂移监控 / 术语聚类 / PDF 标题启发 / sub_checks 导入 | 142→160 |
| A 段（QWK 攻坚） | banding / L2 锚点 / **全 98 篇定论 QWK≈0** | →167 |
| CodeRabbit 审查（2026-06-18） | ~40 bug 修复 + 4 设计项，11 commit（`ec2b624`…`c8fb835`） | 167→**168** |
| 产品化 P1–P5（2026-06-19） | 本地模型一等支持 + 触网/离线自检 + 结构化导出 + 单租户登录 + owner_id(0008) + 部署/上手文档，7 commit（`03d8f82`…`d0679ec`） | 168→**182** |

> 逐轮明细：设计对齐 = 本文件 **git 旧版**；A/B/C 段 = **`执行进展.md`**；CodeRabbit 详情 = git 旧版 §3 + 记忆 `coderabbit-deferred-design-items`。均未删除。

---

## 6. 数据模型与迁移（现状）

迁移到 **0007**。`db/models.py`：`User/Rubric/RubricCriterion/GradingBatch/Paper/PaperChunk/ScoringRun/ScoreItem/ReviewLog/SpreadsheetWriteLog/CalibrationAnchor`。
**本计划预期新增**：P4.3 鉴权（迁移 **0008**：User 登录字段/session、Rubric/GradingBatch/Paper/ScoringRun 加 `owner_id`、created_by/reviewer 回填登录用户）；P3.4 结构化导出（无需建表）。新增端点/字段须配 Alembic 迁移 + 测试。

---

## 7. 推荐执行顺序

**产品最小可发布闭环（按价值/依赖排序）：**
1. **P1 LLM 双轨产品化**（P1.1–P1.4）——产品核心卖点，工作量小、收益大。
2. **P2 CLI 离线闭环**（P2.1–P2.3）——兑现"本地不依赖网络"承诺。
3. **P4.3 Web 鉴权**（单租户 + 简单登录 + 预留 owner_id，§4 已定）——Web 公网上线硬前置。
4. **P3 主流程打磨** + **P5 部署/上手**——体验与可运维。
5. **P4.1 技术债**（retrieval ORDER BY 等）——择机随重锚。

> 其中 P1/P2 大部分是**已具备能力的产品化与文档化**（机制已通），是投入产出比最高的起点；P4.3 鉴权是唯一较大的"需新建"工作量，但 §4 已定**单租户起步**，范围已收敛（简单登录 + 预留 owner_id，非完整多租户）。

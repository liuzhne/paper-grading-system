// 浏览器验收（计划 §12.1 的 V01–V13）尚未接入。
// 这里显式失败，避免把「脚本存在」当成「验收通过」。
console.error(
  [
    "test:e2e 尚未配置。",
    "",
    "计划 §12.1 要求阶段 0 建立浏览器自动化（Playwright）并固定浏览器版本，",
    "覆盖 V01–V13 验收矩阵。在此之前不得把本命令的结果记为通过。",
  ].join("\n"),
);
process.exit(1);
